/**
 * @sporta/contracts — shared contract definitions for the Sporta platform.
 *
 * Placeholder module for W001 (repository bootstrap). W002 will implement the
 * contract domains below as individual modules:
 * - timestamps: canonical timestamp semantics shared by all contracts
 * - identity: session-local subject identity contracts
 * - rights: media rights and authorization contracts
 * - media-session: media session and timeline contracts
 * - observation: raw observation with provenance and confidence contracts
 * - event: versioned event envelope contracts
 * - world-model: sports world model snapshot and event contracts
 * - renderer: renderer request and result contracts
 * - streaming: streaming delivery contracts
 * - football: football sport extension contracts
 */

export const CONTRACTS_PACKAGE_VERSION = "0.1.0";
