# Sporta Work-Item Status

Evidence ledger. Only the tech lead records status transitions here, and only
after verifying acceptance criteria with executable evidence (commit sha,
tests, CI run). Workers report; the tech lead verifies and records.

Status vocabulary: `NOT_STARTED`, `IN_PROGRESS`, `COMPLETE` (evidence
recorded), `BLOCKED` (reason recorded).

## M0 — Foundation

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W001  | COMPLETE    | S001: merge bc18272 (worker commit e4dfad5, session w001-repo-bootstrap); tech-lead re-ran bun test 1/1, lint clean, typecheck 0, format:check clean; CI run 34731407456 success on main |
| W002  | COMPLETE    | S001: worker commit c35bb90 (session w002-domain-contracts); 47/47 tests green, lint/typecheck/format clean, 13 schemas exported + goldens enforced; COMPATIBILITY.md added |
| W003  | COMPLETE    | S001: worker commit 806e649 (session w003-test-ci-foundation); @sporta/testing deterministic builders/sequences, tests/e2e/m0-pipeline.test.ts vertical slice, HARNESS.md + CONTRIBUTING testing conventions; 328/328 green |
| W004  | COMPLETE    | S001: worker commit b338854 (session w004-session-model); 90 session tests + full-suite green; lifecycle machine, fail-closed rights, in-memory + bun:sqlite repositories |
| W005  | COMPLETE    | S001: worker commit 1870cae (session w005-observation-model); 31 observation tests + full-suite green; store, evidence-linked derivation, deterministic replay with corrections |
| W006  | COMPLETE    | S001: worker commit 2445c7b (session w006-swm-contract); 95 world-model tests + full-suite green; versioned entities, at-T snapshots, bounded reorder, football extension |
| W007  | COMPLETE    | S001: worker commit 41803d1 (session w007-observability); zero-dep logger/correlation/metrics/trace + m0-observability e2e (6-stage correlated trace); 356/356 green |

## M1 — Football perception + commentary

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W101  | COMPLETE    | session w101-source-ingestion; branch 37d59f1; merge f1ed65a; CI success; 395/395 tests, lint/typecheck/format clean; TL review: rights-gate-first ordering, magic-byte-only sniffing, frozen idempotent receipts verified |
| W102  | COMPLETE    | session w102-demux-decode; branch 3c4cd71; merge ee06312; 440/440 tests incl. ffmpeg integration; lint/typecheck/format clean; TL review: policy-in-envelope, finally-unlink on 7 paths, bounded stderr |
| W103  | COMPLETE    | session w103-timeline-sync; branch e9b5807; 544 branch tests, 643 integrated; lint/typecheck/format clean; TL review: affine clocks + exact inverse, two-anchor drift ±1ppm, clamp+anomaly, honest driftMeasured=false fallback |
| W104  | COMPLETE    | session w104-segment-transport; branch 02904cb; merge 68b3aa2; CI run 34747782897 success; 674/674 tests (31 transport), lint/typecheck/format clean; TL review: bounded channel (capacity+bytes, block/reject/drop-oldest, never-silent drop accounting), deterministic injectable-clock retries (non-retryable never blind-retried), StageRunner end-to-end bounded memory + orderly shutdown |
| W201  | COMPLETE    | session w201-detection; branch 20d04b0; 550 branch tests, 595 integrated; lint/typecheck/format clean; TL review: exact benchmark math, contract-derived types, zod-validated emission |
| W202  | COMPLETE    | session w202-ball-tracking (conversation ca6ce0a4, landed 16:32 after 7h+ capacity siege via supervisor fighter); branch 0a4a942; merge fec7c5e; CI success; 765/765 tests (33 ball-tracking), lint/typecheck/format clean; TL review: exact exponential confidence decay 0.5^ceil(gapElapsed/4) on interpolated points, never-extrapolate edges, interpolated positions NOT emitted as observations (W205 boundary honored, OBSERVED provenance only), linearly-accumulating association gate documented, id-switch visible by construction, pure/deterministic, 4-case benchmark set with deep-equal rerun |
| W203  | COMPLETE    | session w203-field-mapping (conversation ee80adf2, landed after 4h capacity siege via supervisor fighter); branch 9d26f55; merge 0b83682; CI success; 732/732 tests (58 field-mapping), lint/typecheck/format clean; TL review: exact 8-point DLT with partial-pivot rank check, canonical h[8]=1 form, adjugate inverse with round-trip guarantee, pure never-clamp projector (inBounds flag, out-of-play honest), fixture camera model with documented pan/zoom/jitter math, OBSERVED-provenance field-mapping observations with stable cameraHomographyRef |
| W204  | COMPLETE    | session w204-identity-tracking (conversation 0559869a, landed 16:38 one-create via relay-armed fighter); branch 421d66f; merge 35a7fe1; CI success; 820/820 integrated tests (55 perception-tracking), lint/typecheck/format clean; TL review: greedy-IoU with deterministic 3-key tie-break (IoU desc, detection order, creation order), track ids never reissued, scene-cut hard boundary (close-all), confidence verbatim passthrough, subjectEntityRefs {entityId,kind} with no invented kinds, no velocity (W205/W206 boundary), image-space center position documented, identity-switch + track-purity benchmark |
| W205  | COMPLETE    | session w205-ball-state (re-dispatch #3, conversation 78500d85, landed 21:59; first attempt's completed work lost to sandbox-TTL expiry — lessons 116/117; token placeholder fixed in all staged prompts); branch 0570608; merge 90f1759; CI success; 916/916 tests (42 ball-state), lint/typecheck/format clean; TL review: centered-difference velocity omitted at ends/gap-jumps/degenerate spans (never extrapolated/zero-filled), confidence passthrough verbatim (W202 decay preserved on interpolated), provenance detected→OBSERVED vs interpolated→INFERRED, velocity key present-only-when-defined, subjectEntityRefs {entityId:"ball",kind:"ball"}, trackId visibility preserved (fragmentation evidence), pure/deterministic, exact benchmark assertions (caught a real centered-difference bug mid-work) |
| W206  | COMPLETE    | session w206-spatial-state (conversation 4fac9415; re-dispatched after sandbox reset orphaned the first create at 01:11 — prompt recovered verbatim from dead chat 06280187 via lesson 118; landed 02:38, report 03:02); branch c423557; merge 4db3641; 1086/1086 tests (70 spatial-state), lint/typecheck (21 pkgs)/format clean; TL review: per-frame DLT projector solved once, box-center projection → pitch meters with inBounds flag never clamped, confidence min/product fusion with raw sourceConfidences preserved verbatim, sessionMs timeline alignment (affine slope 1.001 offset +50 exact proof), DERIVED emission with NO velocity + exact LocalEntityRef shapes, exact-math projection tests (52.5/26.25 identity camera), 120-frame pan-sweep e2e (maxPerFrameJump 0.133 m < 3, identitySwitches 0, coverage 1, deterministic rerun), scene-cut semantics (1 switch/object, sessionMs monotone across cut); worker caught a real spec flaw (pan 0.5 zoom 1 is exactly tangent — out-of-play path proven at pan 1 instead, ulp-level honesty, flagged for TL) |
| W207  | COMPLETE    | session w207-stt-adapter (conversation 0697d5ee, landed 19:05 after 2.3h capacity fight incl. one false-VERIFIED void + re-arm); branch 42a09db; merge b02bc3b; CI success; 874/874 tests (54 asr), lint/typecheck/format clean; TL review: provider-neutral AsrBackend seam with lazy z-ai-web-dev-sdk import (package loads offline), WAV encoding for windowed PCM, timestamped TranscriptionUnits with actual-span endMs (never claiming audio that is not there), transcription observations modality "audio" (W209 owns commentary upgrade), confidence omitted-when-absent (never invented), speakerLabel passthrough (W208 owns diarization), bounded AsrError causes |
| W208  | COMPLETE    | session w208-commentary-segmentation (re-dispatch, conversation 2717883c, landed 23:31; first session's turn died silent with sandbox reset — voided, lesson 116 discipline applied); branch 0d6cdfa; merge c596c84; CI success; 968/968 tests (52 commentary-segmentation incl. w207-integration), lint/typecheck/format clean; TL review: rule-based deterministic segmentation (punctuation/speaker/channel/gap, every rule test-pinned), empty units skipped honestly, window-level timing honesty (no invented character timing), modality upgrade audio→commentary claimed as segmentation-not-understanding, provenance DERIVED (deterministic inference), confidence omitted-when-absent, no entity refs (W209 territory), stable seg- ids, pure |
| W209  | COMPLETE    | session w209-commentary-interpretation (conversation fb5c0ccf, landed 00:35, delivered 01:05 — 38 min); branch 15caa8d; merge 9bccc24; PUSHED to main d42a666 after PAT restore (sandbox reset 01:11 had wiped ~/.secrets/env.sh); CI run 33 success on d42a666; 1016/1016 tests, lint/typecheck/format clean; TL review: deterministic event-candidate extraction (lexicon DATA pure, priority chains goal>save>shot / free-kick>foul / corner>pass, span contention + outcome absorption), exact confidence formula 0.5·pattern + 0.3·subjects(0.4 no-subject) + 0.2·(1−0.5·emphasis), subjects/emphasis/observe all test-pinned, 48 tests |

**Gate G2 — Football understanding: ACHIEVED (2026-09-14).** M1 complete
(all 13 items). Exit demo: `tests/e2e/m1-understanding.test.ts` (branch
775392a, merge a33cd79, session g2-exit-demo) — one deterministic fixture
produces synchronized player/ball observations (W204→W203→W206 players,
W202→W205 ball, one shared 40 ms session timeline, one ObservationStore:
120 player + 50 ball vision observations incl. honestly-DERIVED
occlusion-bridged frames) plus commentary-derived event candidates
(W208→W209: 8 candidates, verbatim phrases, hand-derived confidences,
goal confidence 0.91 pinned). Ingested via fail-closed rights lifecycle
(W101-W104 chain exercised through the session model). 1159/1159 tests
at merge; CI green.

## M2 — World model

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W401  | COMPLETE    | session w401-world-fusion (conversation a6e31b7c; dispatched in parallel with G2 demo 03:21, branch pushed 03:59, report 04:12); branch c6c926c; merge 2bf5389; 1158/1158 tests (72 fusion), lint/typecheck (22 pkgs)/format clean; TL review: FOOTBALL_EVENT_MAP 14-entry table exactly per spec (set-piece→restart, foul→referee-decision, fulltime→clock post-match, other dropped-but-counted), fe-<observationId> deterministic event ids make DuplicateEventError the re-fusion dedup, entities with honest uncertainty slots (position uncertain, confidence-absent stays confidence-less, velocity ignored per boundary), EXPLICIT conflict ledger (connected-component window grouping, one record per group, resolution "none", values carry confidences, rerun-identical), possession nearest-participant product confidence with MISSING_CONFIDENCE_DEFAULT + 1e-9 tie→conflict (no silent winner), idempotent re-fusion proven (snapshot version unchanged on rerun), LateEvent/warning paths never silent, W209 generic-payload parser pending future typed contracts bump (documented) |
| W402  | COMPLETE    | session w402-temporal (conversation 00314426, dispatched 04:15, branch pushed 04:41, report 04:55); branch 8d72a87; merge 64d4ab6; 1201/1201 tests (42 temporal), lint/typecheck (23 pkgs)/format clean; TL review: eventWindow inclusive bounds preserving sequence order (pinned no-resort), stateAt engine-equivalence, replayForward fresh-engine with FORCED deterministic now (REPLAY_GENERATED_AT_MS, init.now ignored — pinned), static reverse-scan supersession (X superseded by C → X skipped, C applies detached with net effect identical — worker flagged engine tension honestly, TL-accepted), orphaned corrections counted never silent, duplicate-tolerant, checkpoint boundary-crossing cadence exact, TemporalLimitsError fail-loud, deep-equal rerun, M2 vertical e2e (store→fusion→replay); honest limitations: entities not reconstructible from event windows (upserts are not events — football state caller-pinned as replay init), replay-local watermark sequence |
| W403  | COMPLETE    | session w403-replay-evaluation (conversation agent-e87afc7b; branch 16d6b17 pushed; merge 267f015); 1438/1438 integrated tests (89 new: comparator 26, serialize 13, shape 12, fixture 13, pipeline 10, determinism 4, e2e 10, regen-golden 2, tolerance-doc pin 3), lint/typecheck (25 pkgs)/format clean; TL personally re-ran all five commands + the evaluate CLI: 2 separate bun subprocesses on the sha256-pinned frozen fixture → byte-identical artifacts (246 exact, 15 count, 20 epsilon max|Δ|=0, 12 set arrays) + golden match, VERDICT COMPARABLE exit 0; TL tamper-probe: golden confidence +5.0 → epsilon-breach diff with path, exit 1 (fail-loud proven), golden restored; regen-golden refuses without --confirm (TL-review reminder); TL review: field-classified deep comparator (EXACT/EPSILON/COUNT/SET, first-match rule table, unclassified → fail loud with JSON path, NaN/undefined-vs-missing explicit), forced-constant volatile fields (TEST_EPOCH_MS injected clock, REPLAY_GENERATED_AT_MS), TOLERANCE.md 380 lines pinned row-for-row to code by test; honest limitations: supersession/orphan/duplicate counters compared COUNT-exact but measure 0 (no correction-producing seam in fixture — future work), cross-binary byte-identity not claimed |

**Gate G3 — Sports World Model: ACHIEVED (2026-09-14).** M2 complete (W401, W402, W403). Exit demo: `tests/e2e/m2-world-model.test.ts` (branch a66cfb4, merge 16d4293, session g3-exit-demo) — one deterministic story: 244 vision observations + 5 commentary candidates (conf 0.77–0.91) fused in five streaming waves (per-wave FusionReports exact: upserts 52/48/48/48/48, snapshot versions 55→256, dedups growing 0→4, clock patch on fulltime); `stateAt` at 5 pinned timestamps showing version/provenance/confidence evolution (entity versions 13→25→37→[50 with correction]→62; possession confidence evolving 0.243→0.405; lastSeenMs/spatialFrame honest; score stays unknown until evidence); a REAL correction (video-review STT evidence, conf 0.97, `correctionOf` the goal event) applied through public seams — replay: 4 applied, 1 supersededSkipped, 1 orphaned in the narrow window (both W403-gap counters now measured at 1, never silent), 4 checkpoints all forced REPLAY_GENERATED_AT_MS, final state reconciled with direct query; idempotent re-fusion (version 256 unchanged, auditTrail agrees); whole story ×2 deep-equal. 1656/1656 at merge; CI green. Demo correction linkage is demo-authored policy through public seams — W401 fusion itself derives no corrections (seam gap now VISIBLE; future work item candidate).

## M3 — Offline stylized rendering
| Item  | Status      | Evidence |
|-------|-------------|----------|
| W501  | COMPLETE    | session w501-renderer-contract; branch 4750ce3; merge confirmed; 496/496 integrated; lint/typecheck/format clean; TL review: R1-R8 enforced by 13-check harness + negative mocks; reference renderer isolated from SWM core |
| W502  | COMPLETE    | session w502-anime-renderer (conversation agent-3fbff055; branch 13348f1 pushed; merge 766e7f7); 1438/1438 integrated tests (148 new: palette 142-line file, plugin 635, svg 286, conformance, clip, integration, geometry, captions), lint/typecheck (25 pkgs)/format clean; TL personally re-ran all five commands + live smoke render: 6-frame fixture clip → SVG documents (1170×880 striped pitch, Laws-of-the-Game markings, identity-styled player discs, honest ball opacity 0.935 from confidence 0.9, OUT markers at unclamped coords, caption band) + manifest (per-frame: output timestamp/window, source watermark, appliedEventSequences, captions, possession, EVERY snapshot entity id with explicit disposition rendered/rendered-out-of-play/omitted-no-position/omitted-invalid-position/not-rendered-kind — the W503 flicker-measurement source), rerun deep-equal proven live; conformance harness 13/13 checks PASS (test-pinned); TL review: rendererClass "procedural-3d" (honest fit — pure SWM projection, stylized-video class requires source-frame conditioning; TL-accepted, no contracts change), rights fail-closed BEYOND R2 baseline (carries sourceFrameRefs without canReferenceSourceFrames → rights-denied rejection in BOTH validateRequest and render — pinned incl. deriveRightsCapabilities analysis-only policy e2e), degradation explicit with accounted dispositions (never invented positions/confidence/text), identity stability = pure function of (entityId, version) with fnv1a32 + fixed palette (no-flicker proven by byte-identical marker groups), isolation held (imports contracts/renderer-contract only), no external deps (bun.lock +16 workspace-registration lines only, W402 precedent); honest limitations: 1 fps SVG (encoding = W504), positions held between watermarks on single-snapshot path, captions carry no subject names (verbatim rule), corner arcs/goal frames omitted (documented cosmetic) |
| W503  | COMPLETE    | session w503-temporal-consistency (conversation agent-e61a7dfa; branch fd775e7 pushed; merge 68d121c); 1656/1656 integrated tests (131 new), lint/typecheck (27 pkgs)/format clean; TL personally re-ran all five commands + the evaluate CLI: clean W502 fixture → flicker 0, unexplainedAbsence 0, styleStability 1.0 (12/12 tokens), styleBytes 1.0 (24/24 marker groups), 25 measured pairs, jumpCount 0, maxJumpRatio 0.0639, all 15 artifact counts 0, 21/21 checks PASS + 9/9 injected defects DETECTED each flipping verdict to FAIL (unexplained absence, manifest style instability, SVG byte-level restyle, player teleport 36.7m, ball kind-specific bound, watermark regression, disposition flap, applied-seq gap, duplicate attribution, window overlap); TL review: metrics are pure functions over the W502 manifest, justified dispositions never flagged as flicker, gaps break geometry series honestly (never interpolated — a gap never masks a teleport), thresholds in THRESHOLDS.md pinned to code both directions (PLAYER_MAX_SPEED_MPS 12.5 = Bolt Berlin 2009 derivation, BALL_MAX_SPEED_MPS 40, POSITION_EPSILON 0.01), malformed manifests → TemporalEvaluationError with JSON path; honest limitations: byte-extractor W502-serialization-specific, flap detection one-frame oscillation only, unexplained absence cannot distinguish renderer-drop vs upstream track-loss, applied-gap detects interior-only (prefix/suffix drops unprovable from manifest — candidate W803), evaluation manifest/SVG-level not pixel-level |
| W504  | COMPLETE    | session w504-output-pipeline (agent-696a22e5; branch 352b618 pushed; merge 79c1058); 1994/1994 integrated tests on merged main (210 new: 190 output-pipeline + 20 control-api playback HTTP e2e), lint/typecheck (29 pkgs)/format clean; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/output-pipeline + control-api (additive only) + bun.lock workspace-registration (17 lines); constitution greps clean; TL deep-review of the critical fix: assertPlaybackRights now fail-closes on non-finite nowMs BEFORE any expiry comparison (inherited fail-open hole: expired policy + NaN clock would have served protected bytes — worker found it empirically, fixed, pinned on both store backends incl. ±Inf/non-number/undefined × get+list), policy zod-validated (invalid-policy fail-closed), canStoreDerivatives required with honest expired-vs-ungranted reason split, gate used at all 4 store call sites + pipeline locateAnimeRef; delivery: deterministic animated-SVG segment encoding (SMIL begin/dur derived from W502 windows, last-frame freeze, no scripts/external refs) + verbatim JSON manifest container, byte-determinism pinned (hash/byteLength + rerun byte-identical), sha256 via Bun.CryptoHasher with known-answer pins, segmentId = fnv1a32 of render identity (content drift = fail-loud store conflict), content-addressed artifact store (in-memory + on-disk objects/<id> + meta/<id>.json deterministic paths, reopen continues, idempotent counted duplicates never re-written, integrity-verified reads, limit-rejects-never-evicts) + scope-keyed rights-gated render-segment store (in-memory + bun:sqlite, W004 patterns, NUL-free key space, SegmentConflictError); playback routes GET /v1/sessions/:id/renders/:renderId/outputs[/:segmentId] rights-gated fail-closed (deny BEFORE existence revealed, 403 rights / 404 unknown-render vs unknown-segment / scope-mismatch 500 / structural store failure mapping), e2e golden path: session → real anime plugin render over HTTP → host-equivalence deep-equal proof → encode → store → playback fetch re-hashes to the artifact id (byte-identical) + denial + missing + malformed + NUL-id 400 + determinism ×2 + sqlite/on-disk restart rehydration; worker also fixed second inherited bug: locateAnimeRef renderId-narrowing for non-first scopes (silent [] → direct probe with same-id+same-hash validity, multi-scope + tampered-store negative pinned); honest limitations documented (ENCODING.md: no raster/video transcoding, no audio, 1 fps by construction, SMIL support required, pragmatic-structural frame checks; no reverse content index; single-writer sqlite; failed sidecar write leaves invisible unindexed doc). M3 all items COMPLETE — G4 exit demo landed below |

**Gate G4 — First consumer value: ACHIEVED (2026-09-14).** M3 complete
(W501, W502, W503, W504). Exit demo: `tests/e2e/m3-rendering.test.ts`
(branch bb7a02f, merge a661b45, session g4-exit-demo, 2050 lines, one
test) — one deterministic 6 s authorized clip through the complete M3
chain: fail-closed rights on both planes (authorized policy → real HTTP
session; expired policy → 403 rights-denied at creation; no-policy local
lifecycle → RightsDeniedError); 31 W204 frames × 4 objects → W203 camera
→ W206 pitch meters + W207/W208/W209 candidates (kickoff 0.77 / pass 0.85
/ goal 0.91, hand-derived) fused in six streaming waves into one engine,
per-step stateAt+eventWindow, idempotent re-fusion applies nothing;
original-reference leg = W501 test-card renderer over the same request
surface (5×2 s testcard:// segments from the 6000 ms watermark, pinned);
transformed leg = real W502 anime plugin through the same route with
13-check conformance PASS + 6-step clip with per-frame provenance;
fixture metrics = W503 evaluation PASS 21/21 (flicker 0,
unexplainedAbsence 0, styleStability 18/18, styleBytes 24/24, 20
measured pairs, jumpCount 0, maxJumpRatio 0.2146657 — striker 2.6855
m/s vs 12.5 m/s+0.01 bound, all 15 artifact counts 0, hand-derived);
playback = W504 encode → store (idempotent both layers) → every
anime:// ref resolves via locateAnimeRef → REAL rights-gated routes
serve byte-identical animated-SVG segments (served content re-hashes to
the artifact id, manifest verbatim) + no-storage-rights session renders
fine but 403 on BOTH routes before any byte; whole demo ×2 deep-equal +
double-run in separate subprocesses. 2149/2149 at merge; lint/format
clean; typecheck grep-verified zero TS errors. REAL seam gap surfaced
and measured (not papered over): fusion writes the track position into
state slot `position` while renderer-anime reads `pitchPosition` — a
raw fused snapshot renders with every entity `omitted-no-position`
(MEASURED in-test as evidence); the demo bridges with an authored
verbatim-copy adapter at public seams (value-identity-asserted, version
cost +4/+1 pinned); the one-line real fix (either side) is a TL-owned
backlog item. Relative-path imports for non-root-devDep packages per G3
precedent (documented in-file).

## M4 — Real-time pipeline

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W301  | COMPLETE    | session w301-streaming-ingress (conversation agent-1b3284df; branch fa5769d pushed; merge 5cb4bc4); 1656/1656 integrated tests (86 new: admission 14, fixture-source 14, segments 22, shutdown 9, clock 9, backpressure 6, integration 7, determinism 3, e2e 2), lint/typecheck (27 pkgs)/format clean; TL personally re-ran all five commands + the e2e: accounting balance asserted EXACT (segmentsIn 5 = out 2 + rejected 1 + duplicate 1 + abandoned 1, result.balanced true, service runtime-asserts balance before settling); TL review: LiveSource seam (AsyncIterable pull, injectable, no vendor protocol), FixtureLiveSource deterministic (authored schedule + injected clock), fail-closed rights at admission (start() gates BEFORE source.segments() opened — proven by recording source opened-count 0 on all denial paths; mid-stream rights lapse terminates LOUDLY), timestamps VERBATIM (normalized payload deep-equals AND toBe the authored upstream payload — no copy/rebuild/re-stamp; watermarks carry the payload's own upstream time, out-of-order upstream times pass through; arrivalLagMs MEASURED 1500ms constant never applied), backpressure never-silent (block: bounded-memory proof 60-segment burst segmentsIn holds at 9 capacity 8; reject: typed ResourceLimitError counted+ledger+warn+metric; drop-oldest: cross-boundary identity segmentsOut === received + channel.dropped test-pinned), shutdown accounting (parked send closed-under → ABANDONED with ledger/log/metric/receipt preserved; stop idempotent), idempotency (segmentId key; same-id+content = counted duplicate NOT re-emitted; same-id+different-content = media-invalid conflict; channel-refused segment keeps receipt so re-delivery is idempotent duplicate — Recovery rule), W004 lifecycle integration (created→authorized→ingesting; orderly stop = idempotent cancellation; terminal failure → lifecycle.fail with classified terminalFailureClass); honest limitations: NOT claiming real network protocols (RTMP/SRT/WebRTC/HLS — future LiveSource implementations), receipt = acceptance record not delivery guarantee, malformed refuse-and-continue vs rights/limit/source-death/channel-death terminate, rightsNowMs defaults 0 (production callers inject real epoch), VirtualClock does not advance during backpressure parking (sendWaitMs can read 0 — documented) |
| W302  | COMPLETE    | session w302-processing-queues (agent-81a34469); branch 2841ada pushed; merge aae6f00; 1784/1784 integrated tests (128 new), lint/typecheck (28 pkgs)/format clean; TL personally re-ran all five commands pre-merge AND post-merge (post-merge bun install re-link applied) — scope verified: only packages/processing-queues + bun.lock workspace-registration (15 lines); constitution greps clean (zero Math.random/Date.now/new Date/performance.now — hits are doc comments); TL review: four balance invariants runtime-asserted before settle (in = out + rejected + duplicates + deadLettered + abandoned + dropped; imbalance REJECTS done(), never a lying result), per-stage flow identities + drain-completeness proof, output-queue cross-boundary identity consumerReceived + outputQueue.dropped + outputQueue.size === segmentsOut (W104 drop-oldest stays channel-owned), W104-verbatim policies (block parks producer; reject typed ResourceLimitError counted+ledgered+logged+metered; drop-oldest reconciled from channel's own counter), bounded-memory burst PROVEN (120-segment burst, capacity-2 × 3 stages, inPipeline plateau 11), cancellation drain/cancel modes with abandoned accounting + idempotent stop, watermark checkpoints + resumePipeline skip-mode (duplicates counted, dead-lettered key NOT re-failed) + reprocess mode + corrupt-checkpoint refusal, DLQ non-retryable attempts-1 never blind-retried (W104 rule) + retry-exhausted with consumed-backoff timestamps pinned, whole-story ×2 deep-equal incl. logs/metrics/sleeps + suite double-run in two subprocesses; honest limitations documented (in-memory processedKeys, drain needs receiving consumer, wall-clock ProcessingClock future). Note: worker's "inherited uncommitted work" claim is a reporting artifact — TL observed the worktree CLEAN at dispatch; delivery verified real regardless |
| W303  | COMPLETE    | session w303-gpu-worker-protocol flight 3 (branch 96646f0 pushed; merge a6047ec); 2527/2527 integrated tests on merged main (147 new in gpu-worker), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/gpu-worker + bun.lock workspace-registration; flights 1+2 died mid-work (uncommitted package), flight 3 audited the inherited work and completed it (worker-appended worklog entry pending at merge time — TL evidence stands on the verified delivery); delivery per commit: deterministic dispatcher/worker job-claim-heartbeat-report contract with leases, deadlines, bounded retries, DLQ, and exact never-silent accounting (job envelopes with idempotency keys/priorities/resource requirements/deadlineMs, result envelopes with failure class + attempts + injected-clock timing; worker heartbeat emission with monotone sequence + load telemetry, dispatcher staleness detection → in-flight jobs FAILED-LOUD with timeout classification never silently reassigned; lease with expiry + heartbeat renewal, lease-expired → requeue counted with bounded max-attempts → DLQ, idempotency-key dedupe counted never double-run, idempotent cancellation accounted CANCELLED never lost; retryable bounded-retried with deterministic backoff, non-retryable never blind-retried (W104/W302 rule), per-job deadline on injected clock → timeout classification; every attempt/timeout/retry counted + logged + metered; exact ledger: submitted = succeeded + failed + cancelled + deadLettered + duplicates (+ in-flight during run, 0 at settle), runtime-asserted, imbalance fails loud; event trail submitted/claimed/lease-expired/requeued with fully-isolated envelope copies) |
| W304  | COMPLETE    | session w304-stream-render-orchestration flights 1-5 (flight 5 agent-26282d71; branch e94079b pushed; merge ecb706e); 2918/2918 integrated tests (105 new in render-orchestration), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/render-orchestration + bun.lock workspace-registration (20 lines); flights 1-3 died mid-work (flight 3 hit the 200-turn subagent limit with 13 failures + 3 TS errors + 10 unformatted remaining), flight 4 fixed 12 of 13 then died, TL verified the exact remaining inventory and flight 5 finished: the last failure's root cause (instrumented tracing) was a promise-deletion race — a sink throw rejected the tracked job promise mid-run and the void tracked.then self-cleaning handler deleted it from pendingJobs BEFORE runToSettlement's allSettled scan could see it, so done() rejected via the generic in-flight backstop (fail-loud about the SYMPTOM, silent about the ROOT CAUSE; the never-lying disposition posture held — batch 3 un-disposed, never counted rendered); fix: recordSinkFailure() from the per-job rejection handler (first-wins terminalFailure + stageLogger.error with jobId/batchId/ordinal) with the allSettled scan kept as defensive backstop; delivery: watermark-batched incremental SWM consumption (never whole-history re-reads) through W303 gpu-worker job scheduling (watermark-derived idempotency keys, same-watermark duplicate never double-executes, lease/retry/DLQ inherited, admitted-budget exhaustion → resource-limit terminal) to ordered never-silent-accounted output emission (batch in = rendered + skipped + dropped-by-policy; exact identities fail-loud incl. per-phase skip attribution), bounded queue with W104-verbatim policies + byte-budget admission, skip-stale degradation policy with original watermarks + per-phase metering, burst proof (backlog plateau at capacity), watermark-boundary checkpoints + resume with counted duplicates, whole-story ×2 deep-equal + package double-run; honest limitations: render work in-process by design (W303 executor seam is the real-deployment wiring point), sink-failure logging once per rejected tracked promise, no CI beyond the local battery |
| W305  | COMPLETE    | session w305-webrtc-output flight 3, REPLAY-DISPATCHED (agents-tab GLM-5.3 Full-Stack, operator-watchable per lesson-121 ruling; branch 39d6e43 pushed — inherited flight-1 WIP preserved via git transit c5d2a70; merge e5b5209); 3130/3130 integrated tests (package 139: 87 inherited incl. the 4 interrupted-frontier failures audited+fixed, transport/viewer/e2e suites completed +950/+384 test lines), lint clean, typecheck 35/35 packages rc=0 SERIAL per-package re-run after the parallel root script OOM-killed twice (exit 137 with Chrome+console resident — grep-verified 0 error TS on the COMPLETE serial run), format clean; TL personally re-ran all five commands pre-merge AND post-merge + package suite determinism double-run (139 pass/0 fail/599 expects identical across separate subprocesses); audit-first completion of the interrupted frontier: feat = integrity diagnostics (per-frame sha256 content-hash recompute vs declared, frame-count consistency, typed LiveOutputIntegrityError), answer-side offer grammar (zod AnswerableLiveOutputOffer — structural validation before the accept/reject decision, typed rejects never guesses), settle-includes-parked-receipt (never-silent accounting closes the parked-window hole), structural payload seam; test = the missing transport/viewer/e2e suites the dead flight never wrote (fail-closed liveDelivery rights gate incl. expired-policy denial, typed no-downgrade rejects for protocol/codec/latency, MASTER in=out+rejected+dropped+duplicates+deadLettered+abandoned backstop, W306-namespaced stable metric vocabulary); docs = 165-line package README as the honest boundary index.ts references; honest limitations: no real RTCPeerConnection (in-process binding seam — the injected transport/negotiation modules exist for exactly this), no network ICE/STUN story (vendor-neutral seam kept), browser-end e2e is W704's integration, latency measurement is W306; zod as runtime dep follows the @sporta/contracts precedent |
| W306  | COMPLETE    | session w306-latency-benchmark, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200 — replay console unrecoverable in fresh sandbox); branch a8daeb4 pushed (the "failed"-dispatch orphan's inherited uncommitted ~4.4k-line draft audited line-by-line by the completion worker — real-pipeline wiring verified, no mocks: real W006 WorldModelEngine through W402 stateAt/eventWindow seams, real W304 RenderOrchestrator over the W303 gpu-worker protocol, real W502 anime executor wrapped only by clock-read instrumentation; merge 56cb47b); 3488/3488 integrated tests on merged main (latency-benchmark 85 + eval-harness 132, was 121 — suite shape extended to v2, 4 cases), lint clean, typecheck 38 packages rc=0 serial per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands post-merge; delivery: controlled-fixture live-stream latency benchmark in the injected clock domain — per-stage entry/exit instrumentation (ingestion/SWM-store, render scheduling through the REAL W303 queue, render execution under an authored render-duration model echoed in every report, output emission), p50/p95/max+count per stage AND end-to-end as PURE functions over the recorded trace (nearest-rank, documented + test-pinned), versioned zod-validated machine-readable report + human summary, never-silent accounting (240 frames in = 240 emitted + 0 skipped-stale + 0 dropped + 0 cancelled + 0 duplicate; 140 batches, 50 size-limit splits, peak 13 in-system — bounded, never stream length; imbalance fails loud), eval-harness integration as the live-stream case closing W801's documented gap (case vocabulary + strict parser + executor + report type + fail-loud shape-check + runner dispatch + crash containment + CLI + docs + golden regeneration), SLO candidates from measured evidence (SLOs.md + src/slo.ts test-pinned, 12/12 candidate checks pass in the harness case — W802's formalization surface), determinism (CLI --json double-run byte-identical, canonical-equal to regenerated golden, machine-independent); measured on checked-in fixture w306-live-fixture v1: swm-to-batch p50 102 / p95 4478 / max 5027 ms, batch-queue 0/400/400, w303-schedule 0/4000/4400, render-execution 400/400/400 (authored model), finish-to-emit 0/400/400, end-to-end p50 2029 / p95 8849 / max 9762 ms; frame stages n=240: swm-store-sojourn 2121/4680/5027, end-to-end 3968/9080/9762; congestion point under these bounds = the W303 ready queue, not the 8-slot batch channel (SLOs.md §Interpretation — finding about this configuration); honest limitations: injected-clock domain on a controlled fixture — algorithmic latency structure (watermark-grid waits, bounded-queue sojourns, W303 dispatch wait, authored render-duration model, reorder hold), NOT wall-clock/real-network SLOs (same boundary as W305), virtual-clock interleaving documented+echoed (settleQuantaPerArrival), per-window elapsed readings assert containment never equality, cancel/abandon loss paths need mid-run stop() the driver never makes (W304's own suite covers them), harness case drops the 200+KB trace rows from its measured projection (byte-reproducible from the benchmark's pinned run) |

## M5 — Viewer/product

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W701  | COMPLETE    | session w701-control-api; branch 15cb093; 541/541 tests; real-user HTTP E2E 19/19 (golden path + fail-closed rights + dual-side playback gate); lint/typecheck/format clean |
| W702  | COMPLETE    | session w702-viewer-shell (agent-0f99db01; branch ddb5b47 pushed; merge 9972833 + TL merge-seam fix 2d67300); 2148/2148 integrated tests on merged main (154 new in viewer-shell), lint/format clean, typecheck clean after the seam fix (grep-verified zero TS errors); TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/viewer-shell + bun.lock workspace-registration (16 lines); TL review: headless viewer state machine (idle→connecting→ready→playing→paused→ended/error, transitions test-pinned), ONE typed ControlClient port with two adapters (in-process over real createControlApp + HTTP over real createControlServer with injectable fetch) producing ONE ViewerControlError model, output seam shaped as the W502 clip {frames, manifest} with honest capture-store stand-in + unsupported-output class + documented W705 wiring point (W504 playback routes envelope {content, contentType, hash, manifest}), pure decision modules (dom-plan/detail-plan/pane-signature) with every decision headlessly pinned, serve.ts dependency-free dev static server + browser module graph zero bare @sporta/* specifiers (test-enforced) + serve/parse smoke via real fetch, live output honestly unavailable (W704 owns; constant note, no fake live), renderer list capability-driven (W703 boundary); worker found+fixed 2 real inherited bugs: (1) output-provider error-class reclassification — every non-unsupported wire class was remapped to retryable internal (a future 403 rights denial would show Retry for a permanent denial); now recognized classes pass through VERBATIM (the W705 wiring contract), unrecognized → internal carrying wireFailureClass, pinned for all classes; (2) per-rAF-tick sessions-pane re-render destroyed/rebuilt the create-session form at ~60Hz during playback (focus loss/typed-text reset — the dead flight fixed detail-pane but left sessions pane); now guarded by pure pane-signature memo keys, invariant + sensitivity pinned by 6 tests; TL merge-seam fix (2d67300): viewer error model did not know W504's new unknown-segment failure class (each branch green on its own base; typecheck error surfaced only on merged main and was initially hidden by tail-truncated verification — root typecheck does NOT propagate per-package exit codes, lesson recorded; added unknown-segment to the union/list/label table, not-retryable per unknown-* family, verbatim-passthrough pin added) + processing-queues checkpoint test union typing nit; honest limitations: no real-browser paint E2E (fetch-level smoke only — W705/W706), stand-in output route not rights-gated at read time (viewer core always passes W701 getRender gate first — documented), serve.ts dev-grade, no user auth (W701 trust boundary), capture-store render-id mapping assumes sequential createRender (single-user posture) |
| W703  | COMPLETE    | session w703-style-selection (agent-4e628cea; branch 03c177a pushed; merge 0b7f55a); 2380/2380 integrated tests (35 new: 17 plan, 6 core, 12 e2e), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: ONLY packages/viewer-shell (4 new + 7 modified files, +1586/−30, no bun.lock change); control-api audit: the capability listing seam ALREADY EXISTS (W701 GET /v1/renderers answers full RendererCapability documents from the real registry — renderClass, supportedOutputProfiles, requiresSourceFrames, minSnapshotVersion, identity — pinned deep-equal over the real wire), so W703's delta is purely viewer-side, scope discipline held; delivery: src/selection-plan.ts pure decision layer (deriveRendererOptions(capabilities, rights) → option views with capability verbatim + selectable + machine blockedReason + deterministic note; reasons mirror REAL gates: rights-required (plugin R2), no-output-profiles (W701 request build), unsupported-output (vs the viewer's OWN presentable output kinds — a viewer capability, NOT a renderer list), no-session (fail-closed); precedence pinned), RendererSelectionView + beginRender fail-closed rights derivation + createRender carrying renderer + optional outputProfile + opaque styleId/config VERBATIM, bootstrap renders greyed-out rows (reason + note) and dispatches the payload, ZERO renderer-id literals in the plan layer pinned by a source-scan test WITH a teeth test; selection validation e2e over real servers + real registry + real anime/testcard plugins: unknown renderer and capability-violating profile → the server's real media-invalid messages VERBATIM in the viewer error state, capability-satisfying selection → 200 through the real registry composing to W705 playback (ready, SMIL segment player); style surface honest (no renderer declares variants — none invented; the seam carries a style choice verbatim, pinned end-to-end: styleId + durationMs → real manifest carries styleId verbatim, 3 frames/3000ms); TL review: the grey-out is an affordance not a server denial (pinned: direct testcard createRender succeeds while the viewer greys it out — rendering is compute, presentation is the viewer's call), e2e R2 leg uses a test-fixture plugin implementing the real contract gates (no real renderer declares it today), presentability is profile-level inference (playback path re-hashes/validates everything it serves), minSnapshotVersion stays server-side; honest limitations: bootstrap glue compile-checked only (W702 posture), no real-browser paint E2E (W706); TL-backlog observations added: W701 validateRequest classifies plugin rejections incl. the R2 rights gate as media-invalid 400 (the plugin's rights-denied failureClass stays behind the app seam — pinned verbatim with honest comment) |
| W704  | COMPLETE    | session w704-live-playback flight 3, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200; flight 3's completion report was lost to a canceled dispatch context — evidence stands on the TL battery + review of the full c104913..b0c4e13 diff per the never-trust-reported-numbers standard); branch b0c4e13 pushed (two dead flights preserved via git transit: flight-1 orphan 6beeb69 at 395/395 freeze, flight-2 orphan mid-audit-fix 4d4156a at 412 pass + 1 fail — the interrupted frontier; merge 1519fc1); 3589/3589 integrated tests on merged main (package 414 across 31 files), lint clean, typecheck 38 packages rc=0 serial per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands post-merge + package double-run (414/0/3034 identical across separate subprocesses) — scope verified: only packages/viewer-shell (+1 line bun.lock); delivery: live playback through an injected LiveClient port (live-ports.ts) with an in-process adapter over the REAL W305 LoopbackLiveOutputTransport — the open dance fail-closed on BOTH sides of the seam (viewer-side canDeliverLive rights pre-check: denial never sends a request, never retryable; transport.createOffer runs W305's own gate inside; W704's exact-key check ON TOP of W305's zod grammar: an unknown field anywhere is a typed media-invalid reject naming the field, never a silent strip; answer via the REAL AnswerableLiveOutputOffer + endpoint policy: foreign protocol version typed reject never downgrade, non-svg track unsupported-codec typed reject), first-class live statuses (live-connecting → live-playing ⇄ live-reconnecting → live-ended; one presentation at a time — openLive tears down mounted batch, selectRender tears down live; live content consumed at the live edge, no pause/seek, closeLive is the live equivalent), PURE deterministic reconnect schedule delay(n)=min(500·2^(n-1),4000)ms attempts 1..4 (500/1000/2000/4000, test-pinned as the contract) — no jitter (no honest entropy seam; documented extension point), no timers (injected clock, host-driven tick — a host that stops ticking stops the schedule, the constitution's price paid knowingly), bounded cumulative 4 attempts per attached stream with the post-4th loss TERMINAL attempts-exhausted (never a 5th attempt, never a retry storm), retryability as a fail-closed class table (only connection-lost + transport-failed reconnect; rights-denied/rights-lapsed/negotiation-*/protocol-violation/integrity-violation land TERMINAL whatever the count; UNKNOWN class never reconnects — the W702 verbatim-passthrough lesson applied to the live path), presentation holds its frame during reconnect (honest wait, no fake stall), latency/status surface = injected-domain stream position (buffered vs live edge) with absent metrics staying absent (real-network latency is W306's, SLO formalization W802's), telemetry within W706's closed vocabulary (TELEMETRY.md synced), live-plan.ts pure status surface (headline/status/accounting/degradation/reconnect-countdown/outcome) through the pane-signature memoization pattern, DOM edge compile-checked with the served module graph transpile-pinned + source-pinned isolation (zero bare @sporta/* specifiers, W305 imports type-only — boundary.test.ts), test/live-e2e.test.ts 782 lines through the REAL transport (the W705 posture); docs = LIVE.md 188 lines (the normative boundary doc: composition table, open dance, schedule, class table, §7 boundaries); honest limitations: no real RTCPeerConnection (W305 in-process seam — the adapter/player/backoff/plan/bootstrap layers are the same code a future bridge would run), no real-browser paint E2E (OPEN — the standing W705/W706 boundary) |
| W705  | COMPLETE    | session w705-batch-playback flight 2 (agent worklog 364-389; branch 063f72a pushed; merge 878aed4); 2345/2345 integrated tests on merged main (63 new: playback-provider 24, segment-player 24, encoding-renderer 8, playback-e2e 12, dom-plan +8 segment block, viewer-core +7 outputs-pending/segment-path), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/viewer-shell + bun.lock (+1 workspace-registration line); flight 1 died mid-work (~25 dirty files), flight 2 audited line-by-line and completed: audit found the MAIN DELIVERABLE MISSING (playback-e2e.test.ts referenced by 4 docblocks but nonexistent — wrote it: 12 tests golden walk session → real anime plugin render via registry → encoding wrapper stores host-side → ready with segment player presenting the REAL encoded document (6 frames/6000ms, sha-256 + byte-length re-verified over served bytes, 6 data-frame-index groups + 6 SMIL <set> + 1 fill=freeze, declared-timeline math, smil sync instructions, ONE document never swapped), outputs-list-vs-view agreement, processing states over DECOUPLED host (production posture: outputs-pending → host encode→store with deep-equal render-equivalence proof → ready → plays; second render stays pending), error paths over the real wire (rights-denied 403 VERBATIM both routes + deny-before-existence, unknown-render 404 via core getRender gate, unknown-segment 404, no-storage-rights denial), determinism ×2; REAL SEAM FINDING (TL backlog): W504 LIST route is a pure store projection — unknown render ids answer 200 {segments:[]} (no render-existence check in listRenderOutputsImpl, pinned as intended by W504's own test) — provider+ports docblocks now state the caller contract precisely (outputs-pending = nothing stored under scope; render-EXISTS guarantee = the core getRender gate which always runs first, pinned end-to-end) with an explicit seam pin that a future control-api classification change will flip consciously; delivery: playback-provider.ts (real W504 RenderOutputPort: list + segment routes, envelope parse, client-side WebCrypto sha-256 + byte-length integrity, scope guard, VERBATIM class passthrough, outputs-pending, loud multi-segment refusal), segment-player.ts (pure SMIL presentation: ONE self-animating document, declared-timeline playhead, explicit smil sync instructions only on discontinuities, fail-loud validation incl. container-timing-vs-sourceManifest cross-check), encoding-renderer.ts (host-side encode→store wrapper, r-<n> prediction + rollback), viewer-core outputs-pending state + segment mounting, dom-plan/dom-adapter SMIL controls at the DOM edge (setCurrentTime/pauseAnimations), serveViewer wires the REAL pipeline as renderOutputStore + encoding renderer in the registry, bootstrap default output path = real provider through /control proxy; stand-in remains only for its own tests; audit also fixed: 2 TS errors + 1 lint error (root typecheck non-propagation lesson), STALE serve.test.ts BROWSER_MODULES pin (playback-provider/segment-player added to the zero-bare-imports list), untested segmentViewToDomPlan decision layer (+8 tests); honest limitations: no real-browser paint E2E (W706), WebCrypto required (fail-closed where missing), one segment per render (surplus refuses loud, unsupported-output), serve.ts dev-grade, no user auth (W701 boundary) |
| W706  | COMPLETE    | session w706-viewer-telemetry flight 2 (agent-6e42c1a4; branch 3f80956 pushed; merge 70287b0); 2588/2588 integrated tests (61 new: core 14, events 21, plan 5, sink 14, e2e 7), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: ONLY packages/viewer-shell (21 files, +3441/−20, no bun.lock/root changes); flight 1 died mid-work (~19 dirty files), flight 2 audited every line and completed — verdict: design spec-faithful, constitution-clean, kept; audit found and fixed 3 real defects: (1) POISONED DISPATCH CHAIN — a synchronously-throwing fetch seam made the HTTP sink's chain reject as an UNHANDLED REJECTION (probe: host process DIED) and silently swallowed every later event; now sync throws are one counted transport failure, chain stays resolved, pinned with sync-throw-then-ok fetch test, re-probe clean; (2) serveViewer.stop() never flushed the file sink (programmatic server.telemetry.record() users lost buffered lines); now flush+seals idempotently, pinned; (3) dead vocabulary entry (TELEMETRY_OPERATIONS carried loadOutput which no emitter could ever produce — the core re-wraps player load failures under createRender/selectRender); removed + source-scan pin (TELEMETRY_OPERATIONS ≡ run("...") names in viewer-core.ts both directions, with teeth test — the W703 precedent) + corrected stale real-browser-E2E claims in 7 locations (W706 as scoped = telemetry; browser paint E2E honestly marked OPEN as a TL-backlog item); delivery: schema-v1 event model (6 closed kinds: state-transition, operation-timing, error-occurred with verbatim failureClass + bounded message + one-table remediation hints, rebuffer-stall one per playing-while-buffering episode at the frame-player seam, integrity-verified at the real W504 sha-256 seam, user-feedback closed kinds only), deterministic 1-based sequence ids, atMs in the injected clock domain, opaque sess-<n> correlation; sinks: in-memory (validating, cloned) + node-only JSONL file sink (explicit flush/close, no network, no timers) + dev-grade browser HTTP bridge → dev server /telemetry routes → the real file sink; PRIVACY-BY-CONSTRUCTION: exact-key allowlists make policies/artifact bytes/source frames/renderer payloads/free-form records/user ids UNREPRESENTABLE — pinned across 23 sensitive key names × every kind + scalar-only shape walk + re-validation at every sink AND the route (typed 400 naming the field), scope decision + W804 analytics boundary documented in TELEMETRY.md (new in-package doc, W503 precedent); honest limitations: no real-browser paint E2E (OPEN, not invented), HTTP bridge dev-grade (the port is the contract), error message is the typed model's verbatim text (bounded 2048, hostile-server risk documented), no rendererHealth fact (no honest viewer seam), no consent workflow (W804), GET /telemetry exposes path+counters only; deviations NONE |

## M6 — 3D/game-style renderer

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W601  | COMPLETE    | session w601-scene-projection flight 2 (agent-65c8bb60; branch 82e8f3f pushed; merge fe808e3); 2282/2282 integrated tests (133 new: project 45, validate 45, fixture 13, schema 18, serialize 12), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/scene-projection + bun.lock workspace-registration (16 lines); flight 1 died mid-work (uncommitted package), flight 2 audited every inherited file line-by-line and found 4 real defect classes, all fixed + pinned: (1) runSceneConformance fail-soft honesty bug — provided-but-malformed options.snapshot degraded evidence-gated checks to n/a-PASS, now provided-but-invalid FAILS (n/a reserved for absent evidence; branch order test-pinned); (2) camera-slots-canonical false positive — partial camera-slot selection without the option failed legitimate output, now canonical SUBSET consistency (subsequence, deep-equal constants, reorder/duplicate/tamper teeth) + exact-set with option; (3) golden-fixture test MISSING (docblock referenced nonexistent file) — written: 13 tests incl. scene-golden through the fail-closed S8 gate, deep-equal fresh projection, byte-pinned canonical-form, W002-precedent JSON-Schema export (scripts/export-scene-schema.ts + schemas-golden/scene-specification.json + z.toJSONSchema comparison), W402 caller path end-to-end (stateAt + eventWindow → projectScene → at-T scene, conformance PASS, byte-identical rerun); (4) never formatted + 3 lint + 2 TS errors resolved; TL review: scene schema owned by the package (extends, never modifies, frozen @sporta/contracts zod schemas), disposition vocabulary extends W502's with omitted-non-pitch-frame, FUSION position/spatialFrame ↔ renderer pitchPosition SEAM RECONCILED (precedence pitchPosition > position with known-"pitch" spatialFrame guard, no cross-key fallback — a raw W401 snapshot projects cleanly or is accounted; closes the G4-measured seam from the projection side), participants/officials z=0 documented frame constant, ball z iff carried height (never faked), event markers verbatim timeline-anchored with NO scene position (never invented), camera slots = named geometry only (W604 directs), serializeScene canonical form byte-identity, S1-S8 conformance harness with negative fixtures; honest limitations (CONTRACT.md): no animation/interpolation (W602+), no camera policy (W604), no rendering (W602/W603), no proprietary assets (G7), height/heading slots carried but no current producer writes them, ball z is the SWM's verbatim claim not physics, camera-slot subset mode cannot distinguish dropped slot from selection without the option, schema-export golden structural-only (zod refinements invisible); zod as runtime dep follows the @sporta/contracts precedent (no new external package) |
| W602  | COMPLETE    | session w602-avatar-field flight 3 (agent-078e2888; branch 9bc0b12 pushed; merge 4c94b32); 2813/2813 integrated tests (225 new), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge — scope verified: only packages/renderer-3d + bun.lock workspace-registration (17 lines); flights 1+2 died mid-work (uncommitted package), TL rebased the branch pointer to main so scene-projection was on-branch for flight 3, which audited the inherited work line-by-line: design spec-faithful and KEPT (real deterministic pinhole perspective camera — look-at basis with documented nadir fallback, perspective divide, near-plane Sutherland-Hodgman polygon + parametric segment clipping, WHOLE-FIGURE near-plane guarantee: an entity is never half-drawn, omitted-whole with verbatim meters; field furniture verbatim from the spec's world block incl. 3D goal frames and Law-1-semantics penalty/corner arcs; identity-stable avatar styling fnv1a32 → 8-entry ORIGINAL two-tone palette, pure (styleKey, entityId), version-independent — striker version bumps 3→8 across frames with byte-identical style tokens; billboards with honest facing wedges ONLY when the spec carries heading; elevated-ball depth cues ONLY when the spec carries z (dashed height-unknown otherwise); out-of-play markers at TRUE unclamped positions; top-view footprint mode for steep cameras; per-frame manifest extending the disposition vocabulary with camera-space honesty; camera-slot-aware framing FROM the carried slot, fail-loud uncarried); audit fixed: 2 lint errors, 23 TS errors HIDDEN by the root typecheck's non-propagating exit codes (the grep-lesson: wrong-type imports + non-narrowing union access — fixed with type-guard helpers), 14 never-formatted files (classic dead-flight pattern); completion added test/boundary.test.ts isolation-boundary + constitution source-scan pins (W703/W706 precedent) with teeth tests; TL review: presentation format = deterministic SVG through a REAL projective camera (W502 precedent extended; 3D-interchange-text alternative rejected, decision in RENDERER.md §1), height/heading producer = CARRIED-ABSENT HONEST (never invents; heading drawn only from carried radians, dashed when uncertain; ball elevation only from spec z; cross-frame delta derivation deferred to W603 — documented), dispositions consumed never re-derived, rights fail-closed identical to W502 (carried refs without rights → rights-denied in BOTH validateRequest and render, pinned), rendererClass procedural-3d (1280×720 1fps svg/offline, requiresSourceFrames false), conformance 13/13 PASS through the real plugin + registry seams, smoke render 6 frames ≈18.3-18.6 KB SVG; honest limitations (RENDERER.md §10): no real 3D engine (pinhole + painter's algorithm, billboard/footprint representations), no animation/interpolation (W603), no camera direction policy (W604), 1 fps SVG (3D-encoded delivery future), per-figure painter's occlusion, MIN_MARKER_RADIUS_PX floors marker radii only; TL-noted observations recorded in worklog (shared entity-entry references on single-snapshot path, clip-path watermarkAfter W502 analog, possession-ring divergence only for duplicate-entityId snapshots not producible by engine/contracts) |
| W603  | COMPLETE    | session w603-game-style-renderer flight 2 (agent-ba62389a; branch a15d49b pushed; merge 61a92cb); 2991/2991 integrated tests (73 new: interpolate 22, match 40, benchmark +9, plugin +2), lint/format clean, typecheck grep-verified zero TS errors; TL personally re-ran all five commands pre-merge AND post-merge + cross-subprocess SHA-256 byte-identical package suite — scope verified: ONLY packages/renderer-3d (18 files, +2704/−148, bun.lock untouched); flight 1 died to the SANDBOX RESET #2 but its uncommitted work SURVIVED in the worktree (the TL recovered the directory, re-armed its remote with the PAT); flight 2 audited the inherited work: design spec-faithful and KEPT, defects fixed = 1 real code bug (cut-held frames recorded nonzero fraction, contradicting the documented Render3dMatchInterpolation contract — now 0 for held/observed) + 3 test-authoring bugs (wrong 6-step mental model, goal-marker landing off-by-one frame 22, lastEventSequence 0 vs correct 14) + 2 TS errors hidden by the root typecheck's non-propagating exit codes + 5 unformatted files, all fixes test-pinned; delivery: deterministic interpolated match progression from SWM scene timelines — constant-velocity straight-line motion model between consecutive snapshots' KNOWN positions (RENDERER.md §6, positions INFERRED with manifest pair+fraction per frame, heading/confidence/versions/scene-blocks verbatim from the from-spec, clock/score advance only at snapshot boundaries never ticked by frame time), discontinuity handling HELD never blended (declared scene cuts per the W204 caller-declares posture, disposition changes, missing positions, W503-derived velocity bounds 12.51/40.01 m/s, entity-absent-in-to — each with an accounted heldReason, entities never interpolated into existence), per-segment frame grids with an observed frame exactly on every atMs + R8 tiling + MAX_RENDER_FRAMES 3600 universal fail-loud budget, 5 fps review + 25 fps game profiles added to the capability, 0.2.0 rendererVersion bump = the sanctioned immutable-identity restyle moment (style key re-keys, pinned); conformance PASS through the real plugin + registry seams; honest limitations (RENDERER.md §11): linear inference (no acceleration/turning/physics), no real 3D engine, camera direction W604, scene-correctness evaluation W605, no audio, SVG-only delivery, G7 unchanged |
| W604  | COMPLETE    | session w604-camera-director flight 2, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200); branch 74ff373 pushed (inherited flight-1 WIP preserved via TL rebase-transit 344bba8; merge 424c1ce); 3392/3392 integrated tests on merged main (package 141: 129 inherited audited + 12 new), lint clean, typecheck 37 packages rc=0 serial per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands post-merge — scope verified: only packages/camera-director; audit verdict: design spec-faithful and KEPT (byte-identical to pre-rebase WIP verified via git diff); real defect found+fixed+test-pinned: duplicate candidateId conflation (two W209 candidates sharing one id collapsed through focusByCandidateId into ONE focus window, both accounting entries resolved governed, review driven by whichever replay config survived in the map — never-silent accounting violation; direct() now refuses the stream at admission with candidates-invalid carrying both indexes; pin proves the same stream with distinct ids directs cleanly); completions added (each pinned): selfcheck accounting totality (cutCount recompute + window↔accounting trace, 5 negative-fixture tests + all-five-outcomes clean pass, enforced by the composition's fail-closed admission), test/boundary.test.ts isolation pins (src imports only the 4 declared @sporta/* deps + relative; constitution source-scan with teeth — the W602/W703/W706 precedent), mid-rundown review composition (live, live, REVIEW, live, REVIEW — first review's realized extent shifts later live windows, exact output tiling at the review-tail/live-start boundary), evaluate isolation (planDecisionRecords returns fresh source/decision clones — an evaluator mutating records can never corrupt the plan), docs sync (POLICY.md §4.0/§4.5/§5 value-by-value re-verified against policy.ts + the golden, index.ts barrel, types.ts holdMs, README); delivery: versioned policy DATA document broadcast-classic (possession-following default with zones + hysteresis, pinned event-importance table, replay-emphasis configs), validatePolicy fail-closed, CameraPlan rundown with per-window DECISION RECORDS (rule + verbatim W209 candidate) and total 5-outcome candidate accounting (governed/superseded/below-confidence/no-rule/outside-timeline) — the W605 evaluation surface, direct() pure deterministic (JSON-byte-identical plans, input-order invariance, codepoint tie-breaks), selfcheck invariant harness with stable violation ids (one-selection-per-window, boundaries-respected, live-tiling-total, review-within-timeline, decision records, summary consistency), compose render3dDirectedMatch — one REAL render3dMatch call per directed window through the renderer's own styleConfig.config.cameraSlotId seam, stitched into one rundown (frames renumbered, reviews at the W603 review profile, manifest carrying directed slots + plan provenance); evidence: single-window no-review plan byte-identical to a direct render3dMatch call, cross-subprocess SHA-256 stability of the whole composed output, commentary→W208 units→W209 real extraction→plan→directed render chain test-executed end to end; honest limitations (POLICY.md §7 + one audit-added): rule-table policy not cinematography AI, slot CUTS only (fixed W601 geometry, no camera motion), commentary influence = W209 deterministic candidates only (emphasis carried verbatim, not keyed on), replay re-presents existing match time at 5 fps authors nothing new, offline/hindsight batch direction never live, cumulative frame budget checked after each window's render (fail-loud but lazy) |
| W605  | COMPLETE    | session w605-3d-output-evaluation flight 4 (session B local worker commit a7463ce first-pushed on the canonical branch; DUPLICATE-COMPLETION ARBITRATION: session A's capacity-wall takeover — the replay console's GLM-5.3 session was capacity-blocked 37+ assault rounds — produced a parallel TL completion 61bbec6/8d4ab68 which STANDS DOWN as verified fallback insurance per the lane-coordination collision guard; session B's delivery won on merit: 119 vs 46 package tests, dedicated inject.ts defect-injection seam, deeper validation); merged c0ae5a7 by session A TL after full verification; 4056/4056 integrated tests on merged main (package 119: axes/detection/fixture/report/thresholds-doc/validation), 42-pkg serial typecheck 0 error TS, lint clean, format clean; TL personally re-ran the entire battery on the merged tree (install/test/lint/format/serial-typecheck) AND the package double-run (119/0/3192 identical across separate runs); scope verified: only packages/scene-evaluation (+ bun.lock merge); delivery: six-axis correctness benchmark (score/clock/identity/ordering/scene-state/direction + source-truth layer) over the REAL seams — W601 projectScene ground truth per step, renderer-3d interpolateMatchFrame/statusLine/cameraLabel/stableAvatarStyle expectations, W604 plan-verbatim direction checks; zero thresholds with documented derivation (THRESHOLDS.md pinned row-for-row both directions); never-silent findings with accounted truncation; byte-deterministic reports (in-process ×2 + cross-subprocess SHA-256); CLI with PASS/FAIL exit codes; honest boundaries documented (manifest/state-level, not pixel; no latency; no live evaluation) |

## M7 — Release hardening

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W801  | COMPLETE    | session w801-evaluation-harness flight 3, REPLAY-DISPATCHED (agents-tab GLM-5.3 Full-Stack, operator-watchable per lesson-121 ruling; branch fd71d25 pushed — inherited flight-1 WIP preserved via git transit 087682b; merge 2280688); 3251/3251 integrated tests (package 121: 120 inherited audited + net +1 after suite repairs), lint clean, typecheck 36/36 packages rc=0 SERIAL per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands pre-merge AND post-merge + package suite determinism double-run (121 pass/0 fail/255 expects identical across separate subprocesses); worker's sandbox was REAPED mid-report (stuck-open turn, lesson-104 signature — report previewed in DOM but never committed server-side; TL nudged a continuation, and merged on TL verification per the never-trust-reported-numbers standard); audit-first repairs: 29 TS errors HIDDEN by the root typecheck's non-propagating exit codes (type-guard fixes — the W602 grep-lesson recurring), 2 unused imports, 14 never-formatted files (classic dead-flight pattern); real fix pinned by test: inter-case injected-clock monotonicity in the suite-report self-check — case N startMs >= case N-1 endMs enforced (suite-window containment alone let a swapped-window report pass); cases inventory W403/W503/W601 preserved (detection-proof teeth, thresholds from @sporta/renderer-evaluation, scene fixture through the fail-closed gate); honest limitations: suite runs the checked-in fixtures only (no live-stream case — W306's), golden report is structural (schema-stability pinned, no cross-version diff tooling), worker's own completion report absent (reaped) — evidence stands on TL battery + review of the full 087682b..fd71d25 diff |
| W802  | COMPLETE    | session w802-latency-slos flight 2, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200; flight-1 WIP preserved via git transit 4a5fd9f; branch 1d13478 pushed; merge edd2b3b); 3705/3705 integrated tests on merged main (package 116 across 7 files, double-run identical), lint clean, typecheck 39 packages rc=0 serial per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands post-merge + package double-run (116/0/952 identical); delivery: @sporta/slo — 16 SLO objectives traceable to W306's measured evidence (12 batch rows = latency-benchmark's SLO_CANDIDATES adopted VERBATIM, pinned equal both directions against the real package export; 4 frame rows via the documented FRAME_HEADROOM_RULE formula, recomputed), versioned strict zod input schema pinning the injected-clock domain literals, 33-definition two-tier alert catalog with formula-derived thresholds, pure deterministic evaluation (report → which alerts fire), degradation policy table mapped to REAL machinery (statically imported seams from the six real packages — nonexistent machinery fails the suite, teeth-pinned), strengthened partition invariant (policy triggers must PARTITION the catalog — every alert incl. warnings answered exactly once — a future alert without a playbook entry fails loud by design), SLOs.md normative doc §-pinned row-for-row both directions (code and docs cannot drift silently); audit-fixed defects (each pinned): 17 dangling alert-id triggers in policies (stage-level ids vs metric-level catalog — every evaluateSloCompliance call threw; fixed to the real 33-id vocabulary + the partition invariant), misattributed machinery seam (assertGpuLedgerConsistency vs assertGpuAccounting), ALERT_CATALOG type missing [] (40+ downstream TS errors — the dead-flight never-ran-typecheck signature), test-helper cross-test pollution (shallow-copied stat rows corrupted shared baselines — now deep-cloned + regression pin), test-authoring bug (SLO_CANDIDATES 6 stage rows × 2 percentiles not 12), 12 never-formatted files, bun.lock workspace registration (+19 lines only); every baselineMs pinned to the LIVE benchmark run (benchmark-integration test — the benchmark and the SLO table cannot drift silently); end-to-end: real W306 report → project → validate → evaluate → compliant 16/16; CLI over real bytes → exit 0, byte-identical double run, garbage → exit 3; honest limitations (SLOs.md §7): all numbers in the injected-clock benchmark domain (W306 boundary verbatim — real-deployment thresholds need field measurement, the documented operational gap), one-shot windows only (no rolling compliance/burn-rate/paging — no telemetry pipeline exists), loss alert calibrated to the baseline config (block + skip-stale disabled), warning tier a half-headroom heuristic without operator-noise calibration; TL notes: @sporta/slo is the first package dev-depending on viewer-shell (.ts-suffixed imports require allowImportingTsExtensions mirroring — documented in the commit); the SLO table deliberately NOT re-embedded into the eval-harness suite report (zero verdict delta, decision in SLOs.md §6) |
| W803  | COMPLETE    | session A TL implementation (branch work/sa-w803-insurance 8e48006 pushed; merge into main TL-verified) — the CAPACITY-WALL DECISION: the lesson-121 console dispatch (GLM-5.3 Full-Stack, watchable agents-tab session, packet base 002f39e) fought the peak-hours wall for 2h (17:31-19:30, supervisor-owned assault per the never-wait policy, never accepted server-side) and the deadline chain (W803 -> W806 -> midnight) could not wait; the TL implemented the item directly (the S009 deviation precedent, recorded in worklog S201) with the console session stood down cleanly + operator notified; delivery: packages/quality-gates composing the REAL W503 + W605 evaluations into release gates (re-implements nothing, zero new numeric thresholds — the source packages' pinned threshold documents govern) — temporal-stability (renderer-evaluation evaluateRenderOutput over its real clean fixture), scene-correctness (scene-evaluation evaluateSceneOutput over the real match AND directed fixtures), human-review (the fail-closed record gate: absent/malformed/incomplete -> PENDING-HUMAN-REVIEW never PASS; complete-with-failed-item -> FAIL; the demo record is the pipeline self-check explicitly NOT a human attestation); never-silent accounting reconciled every report; not-runnable blocking gates count as FAIL; versioned GATES.md/REVIEW.md policy pinned row-for-row both directions; deterministic reports in-process x2 + cross-subprocess SHA-256; gate-runner throws land NOT-RUNNABLE at both levels (defense in depth); CLI with SPORTA-RELEASE-GATE marker + exit 0/1/2; 29/29 package tests (detection proofs through the W503 package's own injector, corrupted-scene detection, all four human-record failure modes, throwing-runner containment); 4085/4085 integrated tests on merged main, 43-pkg serial typecheck 0 error TS, lint+format clean, TL personally re-ran the full battery on the merged tree + the gate CLI (PASS); honest limitations: machine gates measure what W503/W605 measure (composition only), human gate records review HAPPENED not review QUALITY, fixture domain not production traffic, real sign-off needs a real record |
| W804  | COMPLETE    | session w804-product-analytics flights 1-2, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200; flight-1 WIP transit 6fd6fd3, flight-2 WIP transit fdf07c6 — the worker died mid-verification with the full delivery present; merged on TL verification per the W801 precedent, history linearized by a pull --rebase over session A's lane-coordination note 416d9f4: final commits be61200+d94c670 on main); 3937/3937 integrated tests on merged main (package 141 across 8 files, TL double-run 141/0/979 identical), lint clean, typecheck 41 packages rc=0 serial per-package (grep-verified 0 error TS), format clean, constitution source-scan clean (no clock/random calls); TL personally re-ran the full battery pre-merge AND post-rebase; delivery: @sporta/analytics — OFFLINE analytics as a PURE DETERMINISTIC function of a RECORDED W706 viewer telemetry stream (schema v1, the JSONL the telemetry-file-sink writes or equivalent recorded values; no collection, no network, no clock, no randomness — analytics only interprets what was already recorded, building collection/transport is ops' world), PRIVACY THROUGH THE ONE GATE: every event enters via @sporta/viewer-shell's REAL parseTelemetryEvent (exact-key allowlists, closed vocabularies, scalar-only shapes — a wider event fails with W706's own rejection reason naming the field; analytics never re-implements the schema so it can never drift wider than W706 — pinned by tests), computeProductAnalytics re-validates every event first, versioned zod report schema + canonical byte-deterministic serialization (serializeAnalyticsReport/deserialize), never-silent accounting (events in = classified + unclassifiable-with-reason — unknown session correlation counted never dropped), the normative FUNNEL.md (funnel stages with evidence sources, boundary attribution rules, complete metric catalog with formulas, privacy scope, honest limits of what the W706 vocabulary can evidence), failure metrics (drop-off attribution by terminal failureClass — verbatim never re-mapped, error-class histograms, rebuffer/stall aggregates), actionability (each failure-class bucket carries remediation pointer or documented owner note), determinism + privacy boundary + funnel-table + report-metrics + emitter-integration tests across 9 files; known limitations per FUNNEL.md: no consent workflow exists (deployment prerequisite, recorded), stages limited to what the W706 vocabulary can honestly evidence, offline computation only |
| W805  | COMPLETE    | session w805-production-observability flight 2, TASK-DISPATCHED local subagent glm-5.3 (environment-reset deviation recorded in worklog S200; flight-1 WIP preserved via git transit 116a991; branch eb3f706 pushed; merge 93e1773); 3796/3796 integrated tests on merged main (package 91 across 7 files, double-run identical), lint clean, typecheck 40 packages rc=0 serial per-package (grep-verified 0 error TS), format clean; TL personally re-ran all five commands post-merge + package double-run (91/0/693 identical); delivery: @sporta/health + docs/observability/PRODUCTION.md — the health DOMAIN MAP with 84 audited registry seams (media 16, queue 24, model 0, renderer 12, delivery 19, infrastructure 13) + 3 telemetry seams + 10 documented gaps, every seam claim re-verified against the sibling package sources by the worker (metric literals, kinds, labels, sites), both-directions vocabulary-completeness pin (every name in the 10 *_METRIC_NAMES blocks + renderer plugin inline literals is a map seam or a documented unobserved exception tied to a real gap — total accounting balances exactly), 24-alert versioned catalog with closed threshold-expression grammar (23 zero-tolerance warnings + 1 critical delivery-windows-failed; 2 W306-derived percentile alerts on metric-generic seams — W802's thresholds slot in), every derivation documented, every runbook anchor exists (test-pinned), pure deterministic evaluation engine with never-silent no-data semantics, pure worst-first health rollup (critical→down, warning→degraded, any no-data→unknown, all-ok→healthy, empty→unknown; lattice down>degraded>unknown>healthy; fail-closed on malformed domain sets — all test-pinned; unknown domain health = unknown NOT healthy), versioned dashboard spec + byte-stable canonical-JSON/Markdown definition renderer (11 panels: 5 metric + 6 health covering all six domains; fail-closed query resolution; no web UI, no SVG theater), fail-closed cross-artifact consistency checks; audit-fixed defects (each pinned): map drift (control_requests/failures_total site corrected to app.ts — http.ts emits via the imported constant), map incompleteness (gpu_job_attempts_total missing — added, 83→84 seams + the both-directions completeness test), per-failure-class label inaccuracies (decode/timeline_sync failures_total), 3 test bugs (zero-denominator numerator omission, model-domain pin uninitialized, catalog-table doc-pin heading spill swallowing 5 truth-table rows as phantom alerts), canonicalJson comment honesty (throws fail-loud, claimed undefined-drop), missing README completed; honest limitations: no deployed infrastructure (definitions + pure machinery only — collection/exposition/backend is ops' world, documented as the gap incl. PRODUCTION.md §9 reconciling FOUNDATION.md's OTel-adapter expectation with the no-backend boundary), model domain unobservable at runtime (unknown by construction), latency thresholds injected-clock algorithmic candidates, memory pressure no real counter (documented gap), registry per-process in-memory, queue depth no registry series (no gauge kind); worker's architectural note recorded: the single biggest observability win available = model-domain runtime seams (world-model corrections, perception coverage — every other stage already meters its never-silent accounting) |
| W806  | COMPLETE    | session A TL review (Owner: Tech Lead per work-items.md) — docs/release/RELEASE-READINESS.md (commit 69cfcd5): the security control-by-control table (rights fail-closed W701, untrusted-media validation, tenant isolation incl. W706's 23-key unrepresentable allowlists, model safety, provider isolation, abuse caps), rights enforcement (W701/W704/W705 + the recorded no-auth gap), performance (W306 measured evidence + W802 formalization, the virtual-clock domain boundary verbatim, zero aspirational numbers), tests (the FINAL battery personally re-run on 08e0240: 4085/4085 + 88,513 expects across 290 files, 43-pkg serial typecheck 0 error TS, lint+format clean; the full battery chain 3251->3488->3589->3937->4056->4085 every number reproduced), docs (24 per-package normative docs each pinned to code, complete 50-item evidence ledger, the recorded no-runbook gap matching no-deployment reality), rollback (encode r-<n> + determinism-as-rollback + the honest no-deployment-rollback gap), known limitations (the aggregated register incl. W803's), RC0-RC4 honest mapping (RC0-RC2 delivered, RC3 the recorded deployment boundary, RC4 = this review), the W803 gate-suite run (SPORTA-RELEASE-GATE PASS with the demo-record honesty note); VERDICT: READY-WITH-GAPS — fully tested, fully evidenced, boundary-honest; the gaps are the recorded operational boundaries (no real-network transport, no real-browser paint E2E, the RC3 deployment/auth/telemetry boundary, the human sign-off that remains a human act) |

## M8 — Productization (W901-W920)

| Item  | Status      | Evidence |
|-------|-------------|----------|
| W901  | COMPLETE    | Worker B flight 3, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died post-commit 8895579, flight 2 died mid-battery — TL transits 035eee6/6360eaf; branch fc641a0 pushed; merge 33ad82b); @sporta/capability — versioned zod capability-response surface (schemaVersion literal 1.0, requestContext echo, auth {authenticated|anonymous|invalid-session + activeRole CONTEXT note}, account roles-as-grants, renderers[] with available|degraded|unavailable + reasonCode + rightsAwareness, modes.live with Simulation-F SCHEMA-ENFORCED (live.available requires transportKind live-network; in-process must report in-process-transport-not-live), modes.batch, quotas[] with used/limit/remaining-null-when-unreadable + exhausted, providers[] (control-plane/storage/queue-cache/compute, ok|degraded|down|unknown + reason + degradedMeaning), content.catalogSurfaces (16 role-experience-matrix surfaces with visibility + reason), overall ready⟺no-reason-codes), exact-key strictObjects + closed vocabularies + schema-level cross-field invariants (fail-closed at the parse boundary), transport-free composition with per-entry degradation on feed failure + missing-rights DENY-ALL + self-re-validation, 8 fixtures covering every deny/degraded path (anonymous/authenticated-no-roles/rights-denied-renderer/quota-exhausted/provider-down/provider-degraded/live-unavailable/partial-availability) regenerated through the losslessness-proofed regen script (canonical bytes → prettier → re-serialization must reproduce canonical or exit 2) + freshness tests (every fixture equals a live buildCapabilityResponse of its scenario); 105/105 package tests ×2 identical (448 expects); 4457/4457 integrated on merged main, lint/format clean, typecheck 45 pkgs + apps/web rc=0 serial (grep-verified); TL code review passed + TL CONTRACT-FREEZE SIGN-OFF (the 5 design decisions in capability README accepted: unknown provider state, overall excludes live, roles in capability response, visibility≠authorization, no timestamps — the seam Worker A's W904 consumes); flight-3 audit-fixed: unused-import lint error (regen script), 29 never-formatted files (fixtures now Prettier-formatted with losslessness proof — the regen-golden precedent), clock.ts docblock misattribution; honest limitations: no transport (W910 wraps it), in-memory feeds, telemetry-neutral by design |
| W902  | COMPLETE    | same lane/session as W901 (branch fc641a0, merge 33ad82b); @sporta/identity — REAL user auth: argon2id password hashing via Bun.password (verify round-trips), accounts behind a store PORT (in-memory this wave; Neon adapter W911), opaque session tokens from an injectable EntropySource (default real crypto.getRandomValues; tests deterministic) stored HASHED ONLY (SHA-256; store never contains plaintext — pinned), expiry via injected clock (constitution: no Date.now in library code), role GRANTS per the role-experience-matrix (Viewer/Creator/Analyst/RightsHolder/Operator) with switching = WORKSPACE CONTEXT ONLY (switchRole validates the account holds the role; active role never feeds authorize() — pinned; switching to an unheld role refused), pure fail-closed authorize() (grants + ownership, unknown → deny), the CONTROL GATE: W701's caller-supplied authorization policy becomes identity-attested (createSession re-attests assertedBy <verified userId>) with denials thrown BEFORE the control app is consulted — the no-existence-oracle property proven by call counters (non-owner denial byte-identical whether or not the session exists; render-output reads gate on session owner before any segment fetch; structural GatedControlApp port with control-api as devDependency only — the W504 playback-port precedent), HTTP surface (register/login/logout/me/switch-role) with typed failureClass+httpStatus errors, timing-equalized no-enumeration login (byte-identical 401s); 89/89 package tests ×2 identical (394 expects) incl. REAL Bun.serve+fetch round-trips (happy paths, wrong password generic, expired via clock advance, revoked/unknown token, role-switch allowed/denied, authority-never-granted, oracle-absence, hashed-at-rest, entropy determinism); honest limitations: no email verification/password reset (deployment-stage), rate limiting W913, Neon persistence W911, hosted-browser sign-in evidence W910/W920, self-registration limited to viewer/creator/analyst (operator/rights-holder grants need an assignment surface), default clock deterministic — production injects real time |
| W903  | COMPLETE    | Worker A flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died ~21:10 after scaffold commits 6194f0b/4a9b7ac; TL transit 221df5f; branch 5094b7b pushed; merge 701d2f5); 4133/4133 integrated tests on merged main (apps/web 48 across 5 files: deferred-surfaces/manifest/navigation/service-worker/shell-wiring), lint clean, typecheck 43 packages rc=0 serial per-package + apps/web `next typegen && tsc --noEmit` clean, format clean; TL personally re-ran the full battery on the merged tree (install frozen, test, lint, format, serial typecheck) AND `bun run build` (✓ compiled, 13 routes: 12 static + /search dynamic) + production-server HTTP checks (/, /live, /watch, /search, /manifest.webmanifest, /sw.js, /icons/icon-192.png, /offline all 200; manifest served as application/manifest+json; no x-powered-by); TL code review passed: deferred-surface honesty pinned to the ux-architecture UX-state vocabulary (loading/ready/processing/degraded/denied/unavailable/failed) with every W903 surface exactly `unavailable` + reason = the missing real data/capability plane (never a simulated failure), SW never caches /api/ or /_next/data/ or navigation responses (documented boundary + tests), manifest complete incl. maskable icon, no @sporta/* engine imports (contract-freeze decision: W901 lands uncontested, W904 consumes it); flight-2 audit-fixed defects (each pinned by tests): 640-1024px search-affordance gap (no search UI at all between breakpoints), skip-link focus ring (outline:none → visible :focus-visible), surrogate-pair-splitting search echo truncation, missing README (added: what exists/deferred/SW boundary/run instructions), typecheck hardened to `next typegen && tsc --noEmit` (clean-checkout failure), route-map integrity tests (declared routes ↔ page.tsx files, no orphans); honest limitations: data-free by design (every data surface honest placeholder naming its work order), offline fallback page loads without build-time CSS/JS chunks on truly cold cache (hashed asset names unknowable to the hand-written SW pre-build; chunks cache after first online load — documented), PWA installability code-verified (manifest+icons+SW+registrar+prerendered wiring) but device-install verification requires the HTTPS host (W910); TL ops note recorded: 4GB box OOM-kills 2/44 packages in a single serial root typecheck — TL battery runs per-package (both affected pass individually) |
| W904  | COMPLETE    | Worker A flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died ~40min mid-rewrite, transit 2ac3add; branch aad9a37 pushed; merge fd1c35b); 4520/4520 integrated tests on merged main (apps/web 111: 48 shell + 63 new — surface-state 8-fixture executable simulation gate, api-routes full auth/capability/catalog/watch/output-gate round-trips, card-model dev-seed honesty), lint clean, format clean, apps/web typecheck clean, build green (13 static + 11 dynamic API routes); TL personally re-ran the full battery on merged main + verified the production-server HTTP path (cookie-jar register/login/me, catalog with 3 real seeded sessions incl. an honestly-denied training session, watch model + REAL SVG render output 20317 bytes sha256-verified 6 frames 6000ms); delivery: /api/capability + /api/auth/* + /api/catalog/* + /api/watch/* routes (TL namespace), server composition root src/server/ (dev in-memory, platform-swappable), real-engine dev-seed (real fixtures through real control-api createSession/createRender executing REAL renders — no fabricated content), capability-driven surface states for Home (live honestly unavailable — Simulation F in-process transport)/Explore/Library (denied|ready per auth+role), auth UX with httpOnly SameSite=Secure cookies + grants-only role switcher, deferred-surfaces module shrunk to genuinely-deferred (search/following/create); audit-fixed: build-breaking bun:sqlite module-scope import in /api routes (Node build workers — lazy seam via src/server/runtime.ts, production start documented as bun --bun run start), 6 lint errors, 23 unformatted files, stale deferred-surface tests; honest limitations: in-process dev backing (restart-wiped; W910-914 replace through src/server/), client-hydrated surfaces with honest loading server HTML, W905 watch-page internals remain flight-1 WIP (next flight), server requires Bun runtime |
| W905  | COMPLETE    | Worker A flight 4, TASK-DISPATCHED local subagent glm-5.3 (flight 3 died ~00:20 after core commit 6483b26 — TL transit 2abb215; branch d4a7eab pushed; merge after W904: watch experience full — real SVG frame player honoring manifest timing (play/pause/scrub, discrete seek snapping — never an interpolated lie), Reality Switcher state machine holding the SESSION constant (Simulation G: client state + same-session output fetch, no page reload; only ready options switch; every rejection carries its real reason — requires-render/no-stored-output/renderer-unavailable/rights-denied; unknown renderers never offered; switches/epoch counters = page-stayed-up witness), timeline + event markers from the real SWM event tail (3 real events on the seeded session incl. football/v1/kickoff conf 0.77), renderer-scoped display-cadence controls, honest unavailable panels (Camera W915 / Highlights this wave); 54 new tests (reality-machine pure transitions incl. synthetic accepted-switch, frame-display manifest-timing model, watch-page derivations, route gating); apps/web 165 tests total, root 4574/0 on the branch, merged-main battery 4674/0 + 7 env-skip; audit-fixed: TDZ render crash (play-loop effect referenced playing before declaration — every ready-render would throw), realities-route readonly mismatches, profile prop nullability, phantom story.eventCount, W904 fixture missing eventTail; HTTP evidence on prod build: /watch 200, /api/watch/sess-1/realities honest per-renderer availability, denied session → every option rights-denied + output read 403 BEFORE existence; honest limitations: one stored output per seeded renderer this wave (accepted-switch via synthetic machine tests), rAF wall-clock display cadence no audio, in-process backing until W912-913 |
| W906  | COMPLETE    | Worker A flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died ~02:30 — TL transit fbabefe; branch 545415d pushed incl. the main-W912 merge 7a455a7; merge aa6ef78); 4771/0 + 16 skip integrated on merged main (50 new / 196 assertions: create-studio 32/134 + create-flow-model 18/62), lint/format/typecheck/build clean; TL verified the FULL FLOW over production-build HTTP (port 3907): anonymous /api/create/options → 401; options 200 (sources derby/friendly/training, renderers with handoff capability honest, upload:false honest — fixture library this wave); rights-preview from the REAL contract {canReferenceSourceFrames true, canDeliverLive false, canStoreDerivatives true, canShare false}; POST sessions → 201 private; POST renders → 202 render-job admitted → poll → succeeded (r-1, ingest stored, outputs image/svg+xml 19733B 6 frames, accounting consumed [swm-snapshot, swm-events] unconsumed [], usage cpu-ms 11/render-requests 1/artifact-bytes 19733, 11ms); preview = the REAL watch surface (authorized watch 200, output document readable); PUBLICATION EFFECTS proven: private → catalog [] + anonymous watch 404 → publish → catalog [sess-1] + anonymous watch 200 → privatize → catalog [] + anonymous watch 404; audit-fixed defects (each pinned): existence-oracle in two inherited tests (existing-not-mine 403 vs unknown 404 — rewrote to the uniform deny-before-existence byte-identical pattern + operator-grant counterpoint reaching the real 404), fabricated usage assertion (cpu-ms honestly 0 under frozen test clock — now reads the descriptor's real units), missing Next route context cascade, unregistered eslint-disable rule; delivery: CreateStudioService (712L) + publication store + /api/create/* routes (options/sessions/renders/jobs/publication — auth-gated, identity-attested rights declarations, compute dispatch with never-silent accounting, publish/private visibility with watch-gate effects); honest limitations: no arbitrary-media upload (checked-in fixture library = the authorized sources this wave, UI states it), studio job index + publication flag in-memory (durable seam = hosted persistence wave), W914 async-compute artifacts not yet R2-mirrored (RenderOutputWriter seam — W912 follow-up), sporta.testcard honestly unavailable via async compute (no W502 detailed surface) |
| W907  | COMPLETE    | Worker A flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died ~03:55 mid-test-writing — TL transit 22ce0d7; branch bcaddd2 pushed; merge 9a004bf wave); 4977/0 + 21 skip integrated on merged main (63 new: 29 role-workspaces + 13 match-lab + 21 workspace-routes), lint/format/typecheck/build clean; delivery: the role→surface map matching role-experience-matrix.md EXACTLY (pinned by tests) — Viewer Home/Live/Explore/Watch/Library · Creator Home/Create/Jobs/Library · Analyst Home/Watch/Match Lab/Clips/Notes · Rights Holder Home/Rights Center/Catalog/Audit · Operator Operations/Jobs/Health/Providers/Audit; grants-only switcher with pending work + safe return (un-granted roles NEVER offered; switching refuses), context-only semantics PROVEN OVER THE WIRE (the operations denial byte-identical before/after a role switch), Match Lab over the REAL watch document (3 eventTail events incl. football/v1/kickoff conf 0.77, transcript windows, renders), Operations reporting the REAL W913 bounded-queue depth (0 of 256 over the wire; null = unreadable never invented), honest deferred surfaces (Clips/Notes/Audit — nothing invented); BROWSER-VERIFIED via agent-browser: sign-in → switcher offers exactly the 5 grants + honesty note → Operator workspace nav Operations|Jobs|Health|Providers|Audit with real compute/queue → safe return → Viewer-only account on /operations renders the real denied state; audit-fixed: buildPendingWork inlined formatting (server badge vs client model drift — now flows through the lib's own formatPendingWork), 10 never-formatted files, post-main-merge stale honesty claim (the 'no queue-depth surface' note false after W913 — the observation moved INTO the shared platform-health module so /api/platform/health and /api/operations serve the SAME numbers by construction); honest limitations: Rights Center read-only (W917 landed after — now merged), no viewer/analyst/rights-holder pending-work data plane (no badge — nothing invented), Jobs/Rights scope follows grants regardless of active workspace |
| W908  | COMPLETE    | Worker A (TASK-DISPATCHED local subagent glm-5.3; flight died post-work — TL transit + verification: branch 5018/0 + 21 skip, typecheck OK; merge 1693ed3); delivery: the UX OPERATIONAL-STATES completion — every surface's state machine audited against the vocabulary (loading/ready/processing/degraded/denied/unavailable/failed) with every state backed by REAL control-plane data; gaps closed: Search wired to the real W916 search API (results + the honest no-match state), Following given its honest state per the data, watch renderer processing states surfaced from the real create-job progress, the AUTH-EXPIRED mid-session state (401 → honest re-auth prompt, never a broken page), GLOBAL provider-degraded notices (capability providers[] → visible degradation); the Simulation A-G EXECUTABLE COVERAGE MAP (test/ux-operational-simulation.test.ts — every simulation A-G covered by at least one test); +41 tests (provider-notice/search/following/processing/reauth derivations); the no-pretend sweep (any fake spinners/incorrect retry loops/unavailable-as-loading fixed; sweep documented); TL battery on merged main: 5018/0 + 21 env-skip, 92964 expects, lint/format clean; honest limitations: provider-degraded notice depends on the capability feed's honesty (in-process dev backing until the hosted providers report) |
| W909  | COMPLETE    | Worker A flight 4, TASK-DISPATCHED local subagent (flights 1-3 died per the known pattern — transit 2e68646, defect fixes 75c2f55, marker-2 2cc6959, flight-3 repairs e4cd63c; flight 4 pushed branch d8c4fb0 then died post-push pre-report; TL forensics S300-wave7-audit + TL battery + merge e214fa6); delivery: the REAL-BROWSER E2E acceptance harness (apps/web/e2e — runner + agent-browser driver + 8 flows + WCAG contrast math, 4314 lines) over the production build: **8/8 flows green, 111 assertions, 0 failed, 0 aborted, exit 0 (run mu3velul)** — a11y-smoke 20/20 (skip link first stop + focus target, labelled landmarks, img alt + role=img aria-label coverage, computed WCAG AA contrast pairs, 30-stop keyboard walk into main), sign-in 15/15 (register → sign-out → real login → wrong-password honest error), rights-denial 9/9 (viewer/anonymous on operator/rights routes: the real 401/403 surfaces, denied data never in the rendered bytes), watch 11/11 (real SVG frames, manifest clock, timeline windows, SWM event markers, honest review-format labeling, real provenance), reality-switch 13/13 (real availabilities with reasons, same-session switch without page reload, aria-live notice), output-playback 8/8 (playback-gated 200 JSON document + artifact-source header, marker jump snaps the playhead, **the transport Play→Pause assertion ACTUALLY EXECUTED**), render 18/18 (the full Create Studio golden path: creator registration → authorized source → rights declaration → dispatch → real progress → succeeded → the stored output exists), role-switch 17/17 (grants-only offers for creator + demo identities, nav narrowing per role, safe return); flight-4 audit-fixed defects: the sticky-header covered-refusal (deterministic instant centering + unit-tested nudge math isCoveredByRefusal/coveringNudgeDelta — the app's CSS smooth-scroll raced agent-browser's hit-test), the web-font reflow click race (clickForOutcome applied to render's registration), the partial-pass honesty gap (recorder.abort(): an aborted flow is FAILED even with passing assertions — run mu3v0hdl exposed it), loading-race ready-stage waits (.player-stage[role='img']); 20 new unit tests (118 expects) — root battery 5064/0 + 21 skip on branch AND merged main (exact arithmetic 5044+20), typecheck/lint/format clean, production build green; evidence per the README convention (final run dir + top-level committed report, no stale failure artifacts); honest limitations: the harness is on-demand (not part of root bun test — no browser in every environment; the pure parts ARE root-tested), flows exercise the in-memory dev composition (the deployed Vercel runtime's Bun-native stores are out of scope there), loopback latency only, headless Chrome via the sandbox's agent-browser CLI |
| W910  | COMPLETE    | Worker B flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died pre-deploy, transit 63e4e60; branch 7c82aec pushed; merge cb27200 after TL union-resolve of apps/web/package.json + bun.lock regen); 4520/4520 integrated tests on merged main, lint/format clean, apps/web typecheck clean, build green ×2; PUBLIC DEPLOYMENT LIVE: https://sporta-flame.vercel.app (Vercel Hobby, project scope ekonplacidegmailcoms-projects, 3rd prod deployment sporta-jh45r45py from 614ebcd) — TL personally verified from the sandbox: / → 200 with Sporta shell HTML + skip-link + manifest link, /manifest.webmanifest → 200 application/manifest+json, /sw.js → 200 application/javascript, /api/platform/health → honest JSON (env beta-personal, providers identity/artifacts/transientState in-memory unconfigured — fail-closed honest states); ROLLBACK VERIFIED: deploy1 (w910-prod-1) → deploy2 (w910-prod-2) aliased → vercel rollback to deploy1 served its marker [2s] → promote deploy2 [2s] → final 3rd deploy current (curl-visible markers, all routes 200 — closes the W806 RC3 rollback gap for the web tier); reproducibility: root-dir deploys (apps/web-subdir deploy fails — documented), vercel.json + .vercelignore + DEPLOYMENT.md runbook (provision → configure with REST v10 env path (CLI env add interactive-broken) → deploy → verify → rollback) + env separation local/preview/beta-personal + W911-13 placeholder env-var table (names only, no values); honest limitations: deployed app is the pre-W904 data-free shell until the next prod deploy (merge cb27200 brings the data-driven surfaces), providers unconfigured until W911-913 supply real values, Git push-driven deploys not connected (CLI-driven documented — later hardening), Vercel Hobby personal/non-commercial boundary documented |
| W911  | COMPLETE    | Worker B flight 5 (ops completion; flight 4 finished the code then died pre-OPS — TL transit d4e290e; branch 1211787 pushed; merge on main), the CODE: env-gated Neon-backed identity in the composition root (pg adapters matching the identity AccountStore/SessionStore/MediaOwnershipStore ports exactly; idempotent advisory-locked migration runner), Node-runtime compat for Vercel (transpilePackages, bun:sqlite loud shim, CryptoHasher shim via node:crypto, runtime-aware scrypt-on-Node KDF, non-poisoning singleton retry), flight-5-fixed defects: scrypt err-null-vs-undefined under Bun (broke hash/verify for every Bun caller; falsy check + inline doc), test-helper SessionStore wiring (session-persistence asserting in-memory — now real pg + 30s WAN timeouts HKG↔us-east-1); the OPS: Neon project sporta-beta (id restless-dream-12397247, org webflix, aws-us-east-1, PostgreSQL 17) provisioned via org-scoped REST (GET /v2/users/me/organizations — the documented working route), migration 0001 identity-accounts-sessions-ownership applied (live schema verified: sporta_accounts/sessions/media_ownership/schema_migrations), DSN only in shell env 0600 + Vercel env (encrypted, env id sDD0xKHWe4sBlInJ) — never committed; integration tests against the real DSN: 5/0/18 (register→fresh-store login, session survives store recreation, tokens stored HASHED — table-inspected sha256-only, expired fail-closed, revocation persists); TWO prod deploys (w911-a sporta-qom12upz6, w911-b sporta-2m0a61jbj serving the alias) with the register→REDeploy→login SAME-credentials SUCCESS + me with the old session cookie against the new deployment = persistence across redeploys proven on the public URL; duplicate register 409, wrong password 401; health flipped identity in-memory/unconfigured → neon/configured/ok; root battery (no env) 4520/0 + 7 loud-skip; DEPLOYMENT.md §6 (org-scoped provisioning, env wiring, migration procedure, forward-only additive schema policy — no down migrations, app rollback never rolls back the DB, beta reset = recreate project); honest limitations: identity-only persistence this wave (artifacts/transientState in-memory until W912/W913), Neon free-tier autosuspend cold-starts, deployed surfaces remain the W903 shell until the product wave redeploys, probe account w911-persist-probe left as living evidence |
| W912  | COMPLETE    | Worker B (TASK-DISPATCHED local subagent glm-5.3; branch 923597c pushed; merge 3cc89f8); bucket sporta-beta-artifacts provisioned via Cloudflare REST (APAC, Standard, PRIVATE BY CONSTRUCTION — no public access), pre-existing W910-era S3 keys verified; hand-rolled SigV4 over node:crypto ~120 lines ZERO-DEP (the SDK exception unneeded); the async W504-semantics R2RenderOutputStore (idempotent counted duplicates, fail-loud conflicts, W504 bounds, rights-gated retrieval, read-side integrity verify — audit caught the genuine sha-256 raw-bytes-hex defect: every getSegment would have failed against a real bucket, fixed node:crypto sha-256); env-gated singleton + SportaServer.artifacts seam (W911 pattern) + dev-seed R2 mirror through the pipeline's own deterministic encodeAnimeClip (segment-id equality asserted; the training policy's deliberately-denied derivative READS made the rights-gated read-back path wrong — covered); watch output route now fetches bytes BACK FROM R2 via 60s server-side presigned GET verifying scope + sha-256 + byte length (x-sporta-artifact-source: r2 | in-memory | honest 503/500 — never silent fallback); integration tests vs REAL bucket WITH env: 7/0/22 (round-trip byte-identical, unsigned 400, expired 403 ExpiredRequest, tampered 403, listing/manifest verbatim + duplicates + conflict, rights-denied-before-existence, idempotent delete) + 9 loud-skips without env; composition tests env-independent 5/0 (mirror, r2-sourced bytes, outage 503, tamper 500, honest in-memory); PUBLIC-URL evidence: health flipped artifacts r2/configured/ok (marker w912-a→w912-b), GET /api/watch/sess-1/renders/r-2/outputs/anime-clip-c0f83b01 → 200 x-sporta-artifact-source: r2 byteLength 20317 sha256 EQUALS contentHash 63796d1f… AND THE SAME HASH AFTER the w912-b REDEPLOY (artifact persists in R2 across deployments); access-control: unsigned 400 body-never-contains-artifact, expired 403, tampered 403, delivery no-identity 401 / non-owner uniform 403 / policy-without-storage 403-before-existence / unparseable 403 / nonexistent 404; TL battery on merged main: 4679/0 + 16 skip (7 Neon + 9 R2), 91143 expects, lint/format/typecheck clean; honest limitations: W914 async-compute artifacts not mirrored to R2 yet (RenderOutputWriter port = documented seam for W913), index/stats single-writer until W913 queue serialization, presigned-delivery route access-controlled but not product-consumed (W905 page uses the JSON route — product surface needs the owned-session creation route), no user-owned-session creation surface yet (delivery positive path proven by real-bucket tests + live server-side round-trip), APAC bucket ↔ iad1 ≈ 100-300ms per server-side fetch, integration residue byte-scale index/_stats objects | |
| W913  | COMPLETE    | Worker B (TASK-DISPATCHED local subagent glm-5.3; branch c97e6a2 pushed — the worker died post-battery pre-report, TL verified the full branch: typecheck OK, build green, root 4835/0 incl. 48 new W913 tests; merge after W916; TL post-merge repairs: prettier over 7 never-formatted files + unused-var); delivery: the Upstash transient-state layer — zero-dep fetch-based REST client (GET/SET/DEL/EXPIRE/INCR/LRANGE/RPUSH/LLEN), env-gated port with in-memory fallback + honest health state (the W911/W912 pattern), bounded render queue (fail-closed admission when full — Simulation E's stop-admitting-before-hard-fail), quota guards (per-user/per-job INCR+EXPIRE windows, exhausted → degraded capability + admission refusal, consumption-past-limit REFUSED with counter rollback), rate limits on auth routes (per-IP/per-account windows, honest 429s), TTL cache (never rights decisions); tests: 33 unit (bounded admission, quota exhaustion→degraded, cache TTL, rate windows) + 10 route-level admission guards + 5 env-gated real-Upstash integration (loud-skip); honest limitations: UPSTASH_REDIS_REST_TOKEN proved DB-scoped/unprovisionable from this sandbox (the honest boundary — the port+fallback shipped, hosted evidence rides the env-configured redeploy; documented in DEPLOYMENT.md), in-memory fallback is per-process |
| W914  | PARTIAL    | Worker C flight 3, TASK-DISPATCHED local subagent glm-5.3 (flights 1-2 died pre-battery — TL transits 5d8bb8c/c0d941c; branch 966871c pushed; merge after W901/W902 = 4457/4457 integrated incl. 130 compute-adapter tests ×2 identical (508 expects); 46-pkg + apps/web typecheck rc=0, lint/format clean) — WAVE-1 DELIVERED: docs/work-items/w914-compute-adapter-audit.md (seam-by-seem audit of the REAL W303/W304/W504 sources: job description, dispatch/ack protocol shapes, W304 derivations render-job-<session>-<ordinal>/deadline 60000/9-identity lattice, W504 sha-256 addressing + UTF-8 byteLength + fail-closed derivative gate, metering for W919, reliability semantics; prioritized gap list G1-G10 with contract-change tags + concrete Wave-2 plan) + @sporta/compute-adapter (versioned zod schemas: ComputeJobDescription/Handle/progress/completion-failure envelopes with accounting totality/usage records/capability descriptors with admission honesty — declaring an unsupported renderer rejected; closed lifecycle vocabulary ALIGNED to the real W303 GpuJobEventType incl. submitted, pinned by source scans — a rename on either side fails the suite; ComputeAdapter TS interface dispatch/poll/subscribe/cancel/meter + Wave-2 provider port shapes; TEST-ONLY in-memory reference adapter loudly marked); audit-fixed defects (8, each pinned): UTF-16-vs-UTF-8 byteLength (non-ASCII inline artifacts would mis-validate — TextEncoder + héllo test), cancel-during-handoff RangeError (cancel wins, W303 supersession posture), uncounted invalidProviderReports, 4 inherited-test TS errors, missing promised test files written, missing README, W303 citation error (submitted IS a member), DEFAULT_IN_MEMORY_LIMITS honesty (64 vs W303's 1000 — the admitted budget only); TL DECISIONS RECORDED: G1 ACCEPTED (keep local W303 shape mirroring — zero change to frozen packages), G2 APPROVED (additive createRenderAsync control-plane surface is Wave-2 C scope — this line is the architecture-lock §14 review); REMAINING for W914 COMPLETE: the real hosted compute provider + control-plane integration (Wave 2); honest note: root-suite first attempt showed 4 pre-existing timing flakes under box contention (138s run; suspect packages pass standalone; clean 81s rerun) — environmental, not code |
| W915  | COMPLETE    | Worker C flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died ~02:30 — TL transit 8608fdb; branch fbde163 pushed incl. main-W912 merge; merge 7a31d27 with TL union-resolve of composition/client-api/globals conflicts vs W906); 4771/0 + 16 skip integrated on merged main (42 W915 tests: 28 inherited flight-1 transport/player + 14 route-level), lint/format/typecheck/build clean; delivery: the REAL-NETWORK LIVE TRANSPORT — SSE over HTTP (browser-native EventSource), wire grammar + pure latency math + transport state machine with bounded drop-oldest COUNTED buffers, real per-tick producer (renderAnimeClip per tick over the dev-seed story timeline, RIGHTS RE-DERIVED EACH TICK), both /api/live routes with strict auth-before-bytes ordering, capability wiring (active+source → live-network available; active+no-source → honest unavailable; inactive → frozen W904 in-process state), honest live-label sweep (nav/page no longer present-tense claim live), DEPLOYMENT.md W915 section; LOCAL REAL-HTTP EVIDENCE (production build, port 3115, SPORTA_LIVE_TRANSPORT=sse, ps-verified killed): /api/live → available true transportKind live-network sources [sess-1 derby]; anonymous stream → 401 JSON BEFORE any byte; unknown session → 404; auth'd curl -N → 21 frames / 82,911 bytes over ~10.5s, story timeline cycling (distinct SVG byte sizes), CADENCE client-observed p50=501ms p95=502ms max=537ms vs configured 500ms; LATENCY MEASURED END-TO-END n=21 p50=1ms p95=2ms max=2ms (loopback — real measurement, unsynchronized clocks documented, never a promise); honest limitations: measured latency is loopback (no deployed latency claim — Vercel Hobby 60s serverless caps documented, the transport env-flag is NEVER set just to light the UI), live source is the dev-seed fixture story (labeled, cycling; generation/cadence/latency real), always-on host for deployed-live = future edge-worker/bare-metal path (documented), WebRTC = future media-server path |
| W916  | COMPLETE    | Worker C (TASK-DISPATCHED local subagent glm-5.3; branch f66457b pushed — died post-push pre-report, TL verified: typecheck OK, root 4833/0 incl. 46 new tests/176 assertions; merge): the catalog CONTENT MODEL — 4-kind visibility (public/private/unlisted/role-scoped) extending W906's publication store compatibly (W906 tests green), requester-scoped discoverability (anonymous → public only; viewer → public+own; rights holder → public+owned+policy-scoped; operator → operational view — the real identity/policy filters), fail-closed unknown-visibility NOT discoverable + no-existence-oracle (uniform behavior for non-discoverable items), REALITY GROUPS (alternate realities of the same match/session linked: one session → renderer-specific outputs grouped — the Reality Switcher's data model made catalog-first), search backend over real fields (labels/story metadata/renderer ids + status/renderer/rights filters) with /api/catalog/search + /api/catalog/realities routes; delivery: catalog-service 594-line extension + publication store 242-line content model + 829-line test file; honest limitations: UI wiring of the reality-grouped/search surfaces rides the product wave, search is exact-match text (no ranking/fuzz — documented) |
| W917  | COMPLETE    | Worker B (TASK-DISPATCHED local subagent glm-5.3; worker died post-implementation pre-report — TL verified the branch: 4920/0 after the TL orphan-route repair, typecheck OK; branch 0fcc539 pushed; merge 9a004bf wave with TL hand-union of api-types/client-api/globals vs W907); delivery: the RIGHTS/PUBLICATION CENTER — rights-policy layer (effective-policy store + append-only policy audit log + rights-governed control decorator + RightsCenterService + /api/rights/* routes: policies list/inspect, policy PATCH, visibility PATCH, revocation POST, audit GET — all composition-wired), narrow-only intersection in the governed control plane (a policy edit can only NARROW what the creation-attested policy allowed — never widen), inspection/change/revocation by authorized rights holders ONLY (the real grant + ownership; no existence oracle for non-owners), revocation semantics: revoked content STOPS playback AND publication (the fail-closed gate re-derives; catalog drops discovery; pinned: publish → revoke → anonymous watch 403/404 + catalog absent + bearer watch denied), the Rights Policy Console UI (/rights/policies — API-driven editing surface); 39 tests/175 assertions incl. the revoke lifecycle; TL repair: the /rights/policies route-map declaration the dead flight omitted (caught by the W903 orphan-route integrity test — the test did its job); honest limitations: policy-change audit in-memory dev backing (documented), UI editing surface is the console page (further polish rides the product wave) |
| W918  | COMPLETE    | Worker C (TASK-DISPATCHED local subagent glm-5.3; worker died post-implementation pre-report — TL verified the branch: 4896/0 after TL repairs, typecheck OK; branch 6c4549a pushed; merge 9a004bf wave with TL union of operations-service [W907 document fns + W918 console class coexist], page [W918 console], navigation dedup, api-types/client-api/composition); delivery: the OPERATIONAL CONSOLE — /api/operations/* panels ALL operator-grant-gated (provider-health.read — the real 401/403 paths for everyone else): health (the honest platform snapshot + compute + live transport), queues (the W913 bounded render queue: depth vs hard bound, per-job admission views, admission refusals at the studio dispatch seam), providers (usage ONLY where a real seam exposes it — R2 never-silent stats, compute accounting identities, queue depth; HONEST unknown where none exists; documented free-tier limits ride as the W919 seam never measured facts), jobs (the compute ledger incl. FAILED with never-silent reasons), REMEDIATIONS safe-only (retry re-dispatches through the REAL studio path — NEW job id, full W913 admission ladder, quota charged to the OPERATOR, never a status flip; cancel via the adapter's real idempotent cancel) + the remediation audit trail; the OperationsConsole UI (operator workspace); 15 tests/421 assertions; TL repairs: the /operations route-map declaration (orphan-route catch) + prefer-const + prettier over 4 dead-flight files; honest limitations: Neon exposes no usage counter over our seam (honest unknown — documented for W919), Upstash REST port has no INFO/SCAN (honest unknown) |
| W919  | COMPLETE    | Worker B flight 2, TASK-DISPATCHED local subagent glm-5.3 (flight 1 died mid-edit — TL transit d76e7ab with the branch broken 12 fails + 2 TS errors; branch d639872 pushed incl. the main-W908 merge; merge b6b2af4); TL battery on merged main (CLEAN ENV — the documented stray-DATABASE_URL hazard: postgres.js hangs 10s with the sandbox's file: DSN; env -u DATABASE_URL): 5044/0 + 21 skip (26 W919 tests), 93119 expects, lint/format/typecheck/build clean; delivery: the COST/USAGE GUARDRAILS — provider usage counters over the REAL seams (compute metering cpu-ms/render-requests/artifact-bytes; R2 store never-silent stats; Upstash command port-meter; queue depth) into per-provider windowed records (daily/user + global), the free-tier limit LEDGER (documented limits with checked-date sources; configurable thresholds: SPORTA_LIMIT_COMPUTE_CPU_MS_DAY etc.) surfaced in capability quotas[] + the operations console panel, SPEND ALARMS (under → approaching → reached window-scoped persisted transitions; recovery = a transition) flowing into the capability response (reached → degraded with reason — Simulation E) + console health, FAIL-CLOSED ADMISSION at hard limits (per-user quota AND global capacity; refusal 503 with the real reason + retry-after; reads-before-writes — the render-requests quota not charged on refusal; queue depth unchanged), EXISTING AUTHORIZED PLAYBACK UNAFFECTED proven (byte-identical 200 pre/post-refusal); flight-2 audit-fixed: CommandCounter.flush snapshot-after-read (first flush persisted 11 not 10 — now proven 10 → 12 with buffer=2), console compute rows always-unmeasured (per-user evaluation with userId + plane totals without — honest meterNote naming the scope), a fabricated video/* content-type assertion (replaced with the real W504/W902 contract + byte-identical playback proof), 2 mid-write TS errors + lint/prettier; METERS INVENTORY (metered, admission-enforced: R2 stored bytes, Upstash commands, compute cpu-ms/artifact-bytes per user/day; metered, alarm-only: Neon storage, queue depth; HONESTLY UNMEASURED never estimated: R2 Class A/B ops, Upstash data-bytes, Neon CU-hours); honest limitations: counter persistence read-modify-write (port lacks INCRBY — concurrent writers can undercount; admission stays fail-closed on unreadable), alarms observation-driven (no serverless scheduler), in-memory fallback per-instance, console compute rows plane aggregates (conservative) |
| W920  | COMPLETE    | TL-owned final gate, closed across two sessions (gate-1 found the per-instance defect → W921; gate-2 this session): final redeploy from main @ edd0953 (pushed 48525d6..edd0953; marker w920-final-3) → THE GATE CAUGHT A SECOND REAL DEFECT: intermittent 500s on watch/studio reads under cold parallel bursts (5/12 → 6/20 → 19/24 — escalating as bursts deepened the rate-limit hole) — root-caused by first landing the error-boundary OBSERVABILITY fix (61b8bc9: unknown errors logged with name/message/stack + errorId riding in both log line and 500 body; the lazy control-api error-class import guarded): R2 HTTP 429 — the write storm. Every cold instance's dev-seed mirror and every durable reconstruction re-PUT the IDENTICAL document + scope index + global stats (3 Class A writes per duplicate; content-addressed segments are immutable); a cold-start burst spawned many instances at once → R2's per-account rate limit → fail-loud mirror turned READS of already-durable data into 500s. FIX 54ea626: the duplicate path is READ-VERIFY-ONLY (reads the stored document, verifies recorded hash = offered hash — the whole integrity contract — answers "duplicate" with ZERO writes); genuine first-time stores still write; conflicts still fail LOUD; REGRESSION PINS (r2-write-storm.test.ts, 3 tests): first store = 3 PUTs, duplicates (sequential AND 12-parallel burst) = ZERO writes, composition RE-BOOT against a populated bucket writes NOTHING. Post-fix deployed evidence (marker w920-final-5): 30/30 ×3 consecutive parallel bursts + the full W921 probe 12/12 (register→create→render→cross-instance reads→byte-identical 32260B outputs×2→publish→anonymous catalog→anonymous watch; controlPlane neon/ok). FRESH-BROWSER WALKTHROUGH (agent-browser w920-walk-2, headless Chrome, public URL): sign-up w920-final-walker WITH creator grant through the real form → auto-signin → Library; discover (honest shelves: NOT-LIVE with reason, renderer statuses honest, seeded cards); watch sess-1 (manifest clock frame 1 of 6, 3 event markers, review-format label); Reality Switcher SAME-SESSION (window marker survived, testcard honestly disabled with the real no-stored-output reason); CREATE the full guided flow (derby → rights with server-derived preview → anime.prototype → recipe → review → render) — JOB SUCCEEDED with never-silent accounting (consumed swm-snapshot+swm-events, 5ms queue 1ms, 5 cpu-ms, 1 render-request, 19733 artifact-bytes, image/svg+xml 19733B 6 frames, stored r-u-…); VIEW THE RESULT (preview frame + Open in Watch → the user session plays cross-instance — the original gate failure, now green); role switch (Creator workspace, grants-based); honest degraded/denied states throughout (sess-2 UNAVAILABLE with the real reason; camera honest for stored renders; operations console real 403 for non-operator; live transport honest); no console errors. Footer stale under-claim swept to the public-beta truth. Battery: root 5103/0 + 26 skip / 93494 expects, apps/web 570/0 + 26 skip, typecheck/lint/format clean |
| W921  | COMPLETE    | Worker B flights 1-11 (nine dead transits; flight-9 WIP preserved eaea196 + flight-10 seams/tests 350245b; flight-11 TL-completed the poisoned-row blast-radius fix 4db82f2 + the pre-existing viewer-shell failure-class widening 3d58141); merge edd0953 (tree-identical to branch tip); delivery: DURABLE CONTROL PLANE (Neon) — user sessions/renders/publication write through to Neon at the real studio seams (fail-loud) and reconstruct on instance miss via deterministic replay through the REAL seams (recorded ids + RECORDED createdAt via the additive control-api seams d7056dd, content-addressed segment-id assertion, once per session per instance); collision-safe sess-u-/r-u- ids; bounded blast radius (ControlReconstructionError: per-ROW data failures skip+count in listings — one unreplayable row never 500s the whole catalog — while direct reads fail loud with the real reason; infrastructure failures propagate); honest boundaries (per-instance job ledger, per-instance rights EDITS, unrecorded dev-seed namespace) documented in DEPLOYMENT.md §8 (eda8b6f); evidence: hermetic decisive two-composition-instances-over-one-record-store test 12/12 (83 expects) incl. poisoned-row + expired-policy + fail-loud write-through suites; real-Neon integration 3/3 (32 expects, production database); deployed public-URL probe ×2 (12/12 parallel fresh-connection reads 0 failures, byte-identical outputs, publish→catalog→anonymous watch); TL battery root 5100/0 + 26 skip / 93458 expects, apps/web 567/0 + 26, control-api 98/0 ×2 identical, lint+format clean, all 47 package typechecks clean (serial — parallel OOMs at 4GB) |
